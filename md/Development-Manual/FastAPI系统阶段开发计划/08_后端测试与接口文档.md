# 08_后端测试与接口文档

## 一、阶段目标

本阶段目标是在后端主要业务模块已经完成的基础上，补充稳定的自动化测试、检查 OpenAPI 接口文档，并整理后端交付给前端前的接口验收清单。

完成本阶段后，后端应具备以下能力：

1. 自动检查核心接口是否出现在 OpenAPI 文档中。
2. 自动检查初始化角色和权限是否完整。
3. 自动检查核心 Schema 字段校验是否生效。
4. 自动检查统一响应结构是否稳定。
5. 可以导出 `openapi.json` 供前端、Apifox 或 Postman 使用。
6. 有一份后端接口验收清单，方便进入前端阶段前逐项确认。

## 二、本阶段开发顺序

```text
1. 创建 OpenAPI 路径测试
2. 创建权限种子测试
3. 创建 Schema 校验测试
4. 创建统一响应结构测试
5. 创建 OpenAPI 导出脚本
6. 创建接口验收清单
7. 运行 pytest 和导出 OpenAPI 文档
```

## 三、本阶段测试策略说明

本阶段先补充不依赖真实数据库写入的自动化测试。

原因：

1. 当前项目使用 MySQL，直接在自动化测试中写真实开发库，容易污染你手动测试的数据。
2. SQLite 内存库和 MySQL 在 `BigInteger` 自增、日期时间、外键行为上并不完全一致，直接替换数据库容易出现假通过或假失败。
3. 当前阶段更重要的是确认接口注册、文档生成、权限种子、字段校验和响应格式稳定。

因此本阶段自动化测试重点是：

1. OpenAPI 路由完整性。
2. 权限种子完整性。
3. Schema 字段校验。
4. 响应结构契约。

真实 CRUD 接口仍通过 Swagger 或 Apifox 按验收清单进行人工联调。后续进入更完整测试体系时，可以再单独引入 MySQL 测试库。

## 四、本阶段文件清单

| 序号 | 相对路径 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `backend/tests/test_openapi.py` | 新增 | 检查 OpenAPI 核心路径和认证配置 |
| 2 | `backend/tests/test_seed_permissions.py` | 新增 | 检查角色与权限种子 |
| 3 | `backend/tests/test_schema_validation.py` | 新增 | 检查核心 Schema 校验 |
| 4 | `backend/tests/test_response_contract.py` | 新增 | 检查统一响应结构 |
| 5 | `backend/scripts/export_openapi.py` | 新增 | 导出 OpenAPI JSON 文件 |
| 6 | `backend/docs/api_checklist.md` | 新增 | 后端接口验收清单 |

## 五、步骤 1：创建 OpenAPI 路径测试

### 相对路径

```text
backend/tests/test_openapi.py
```

### 完整代码

```python
from app.main import app


def test_openapi_contains_core_paths() -> None:
    schema = app.openapi()
    paths = set(schema["paths"].keys())

    expected_paths = {
        "/api/health",
        "/api/auth/token",
        "/api/auth/login",
        "/api/auth/me",
        "/api/auth/permissions",
        "/api/auth/menus",
        "/api/system/roles",
        "/api/system/permissions",
        "/api/users",
        "/api/stores",
        "/api/course-categories",
        "/api/courses",
        "/api/action-categories",
        "/api/actions",
        "/api/content/slogans",
        "/api/content/events",
        "/api/home",
    }

    missing_paths = expected_paths - paths
    assert not missing_paths, f"missing openapi paths: {sorted(missing_paths)}"


def test_openapi_has_security_scheme() -> None:
    schema = app.openapi()
    components = schema.get("components", {})
    security_schemes = components.get("securitySchemes", {})

    assert security_schemes
    assert any(
        scheme.get("type") == "oauth2"
        for scheme in security_schemes.values()
    )
```

### 注意事项

1. 这个测试不会连接数据库，只检查 FastAPI 已注册的接口文档。
2. 如果新增业务模块后忘记注册路由，这个测试可以较早发现。
3. 如果后续接口路径调整，需要同步修改 `expected_paths`。

## 六、步骤 2：创建权限种子测试

### 相对路径

```text
backend/tests/test_seed_permissions.py
```

### 完整代码

```python
from app.db.seed import DEFAULT_PERMISSIONS, DEFAULT_ROLES


REQUIRED_ROLES = {
    "admin",
    "store_admin",
    "coach",
    "member",
}


REQUIRED_PERMISSIONS = {
    "user:list",
    "user:create",
    "user:update",
    "user:delete",
    "role:list",
    "role:create",
    "role:update",
    "role:delete",
    "permission:list",
    "store:list",
    "store:create",
    "store:update",
    "store:delete",
    "course:list",
    "course:create",
    "course:update",
    "course:delete",
    "action:list",
    "action:create",
    "action:update",
    "action:delete",
    "event:list",
    "event:create",
    "event:update",
    "event:delete",
    "slogan:list",
    "slogan:create",
    "slogan:update",
    "slogan:delete",
}


def test_default_roles_are_complete() -> None:
    role_codes = {role_code for role_code, role_name in DEFAULT_ROLES}

    assert REQUIRED_ROLES.issubset(role_codes)


def test_default_permissions_are_complete() -> None:
    permission_codes = {
        permission_code
        for permission_code, permission_name, menu_path in DEFAULT_PERMISSIONS
    }

    assert REQUIRED_PERMISSIONS.issubset(permission_codes)


def test_permission_codes_are_unique() -> None:
    permission_codes = [
        permission_code
        for permission_code, permission_name, menu_path in DEFAULT_PERMISSIONS
    ]

    assert len(permission_codes) == len(set(permission_codes))


def test_menu_permissions_have_expected_paths() -> None:
    permission_paths = {
        permission_code: menu_path
        for permission_code, permission_name, menu_path in DEFAULT_PERMISSIONS
    }

    assert permission_paths["user:list"] == "/users"
    assert permission_paths["role:list"] == "/system/roles"
    assert permission_paths["permission:list"] == "/system/permissions"
    assert permission_paths["store:list"] == "/stores"
    assert permission_paths["course:list"] == "/courses"
    assert permission_paths["action:list"] == "/actions"
    assert permission_paths["event:list"] == "/content/events"
    assert permission_paths["slogan:list"] == "/content/slogans"
```

### 注意事项

1. 这个测试可以防止后续修改 `seed.py` 时漏掉权限。
2. 新增模块后，要把模块权限加入 `REQUIRED_PERMISSIONS`。
3. 菜单路径只检查有前端菜单含义的权限。

## 七、步骤 3：创建 Schema 校验测试

### 相对路径

```text
backend/tests/test_schema_validation.py
```

### 完整代码

```python
import pytest
from pydantic import ValidationError

from app.schemas.action import ActionCreate
from app.schemas.content import ActivityEventCreate, SloganCreate
from app.schemas.user import UserCreate


def test_action_create_rejects_invalid_difficulty() -> None:
    with pytest.raises(ValidationError):
        ActionCreate(
            action_name="杠铃卧推",
            category_id=1,
            action_difficulty=6,
            applicable_store_type=1,
        )


def test_action_create_rejects_invalid_store_type() -> None:
    with pytest.raises(ValidationError):
        ActionCreate(
            action_name="杠铃卧推",
            category_id=1,
            action_difficulty=3,
            applicable_store_type=3,
        )


def test_slogan_create_rejects_invalid_status() -> None:
    with pytest.raises(ValidationError):
        SloganCreate(
            slogan_name="首页主标语",
            slogan_content="用每一次训练，靠近更强的自己",
            status=2,
        )


def test_activity_event_create_accepts_date_string() -> None:
    event = ActivityEventCreate(
        title="JoyFit 夏季力量挑战赛",
        event_date="2026-07-18",
        location="上海 JoyFit 徐汇店",
        status="报名中",
        description="面向会员开放的力量挑战赛。",
    )

    assert event.event_date.isoformat() == "2026-07-18"


def test_user_create_rejects_bcrypt_unsafe_password_length() -> None:
    with pytest.raises(ValidationError):
        UserCreate(
            role_id=1,
            username="too_long_password_user",
            password="x" * 73,
        )
```

### 注意事项

1. `bcrypt` 对密码有 72 字节限制，所以用户密码 Schema 限制为最大 72。
2. 动作难度限定为 1 到 5。
3. 动作适用门店类型限定为 1 到 2。
4. Slogan 状态限定为 0 或 1。

## 八、步骤 4：创建统一响应结构测试

### 相对路径

```text
backend/tests/test_response_contract.py
```

### 完整代码

```python
from datetime import date, datetime

from app.utils.response import fail, success


def test_success_response_contract() -> None:
    body = success(data={"name": "JoyFit"})

    assert body["code"] == 200
    assert body["message"] == "success"
    assert body["data"] == {"name": "JoyFit"}


def test_fail_response_contract() -> None:
    body = fail(message="permission denied", code=403)

    assert body["code"] == 403
    assert body["message"] == "permission denied"
    assert body["data"] is None


def test_response_json_encodes_date_and_datetime() -> None:
    body = success(
        data={
            "event_date": date(2026, 7, 18),
            "created_at": datetime(2026, 7, 18, 9, 30, 0),
        }
    )

    assert body["data"]["event_date"] == "2026-07-18"
    assert body["data"]["created_at"] == "2026-07-18T09:30:00"
```

### 注意事项

1. 所有接口响应应统一为 `code`、`message`、`data`。
2. 日期和时间会被 `jsonable_encoder` 转成字符串，方便前端消费。
3. 如果以后修改统一响应结构，这里要同步调整。

## 九、步骤 5：创建 OpenAPI 导出脚本

### 相对路径

```text
backend/scripts/export_openapi.py
```

### 完整代码

```python
import json
from pathlib import Path

from app.main import app


OUTPUT_PATH = Path("docs/openapi.json")


def main() -> None:
    schema = app.openapi()
    OUTPUT_PATH.parent.mkdir(parents=True, exist_ok=True)
    OUTPUT_PATH.write_text(
        json.dumps(schema, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    print(f"OpenAPI schema exported to {OUTPUT_PATH}")


if __name__ == "__main__":
    main()
```

### 运行命令

确保当前目录是：

```text
FastAPI_Vue3_JoyFit/backend
```

执行：

```bash
python scripts/export_openapi.py
```

运行后生成：

```text
backend/docs/openapi.json
```

### 注意事项

1. 运行脚本前不需要启动 Uvicorn。
2. 该脚本只读取 FastAPI 路由和 Schema，不会主动写业务数据库。
3. 前端可以把 `openapi.json` 导入 Apifox 或 Postman 查看接口。

## 十、步骤 6：创建接口验收清单

### 相对路径

```text
backend/docs/api_checklist.md
```

### 完整内容

```markdown
# JoyFit 后端接口验收清单

## 1. 启动检查

- [ ] 已进入 `backend` 目录
- [ ] 已激活 `.venv`
- [ ] `.env` 数据库配置正确
- [ ] MySQL 服务正常运行
- [ ] 后端启动成功
- [ ] Swagger 可以访问：`http://127.0.0.1:8000/docs`

## 2. 登录认证

- [ ] `/api/auth/token` 可以获取 Swagger 授权 Token
- [ ] `/api/auth/login` 可以返回统一响应结构
- [ ] `/api/auth/me` 可以返回当前用户
- [ ] `/api/auth/permissions` 可以返回当前用户权限
- [ ] `/api/auth/menus` 可以返回当前用户菜单

## 3. 系统权限

- [ ] `/api/system/roles` 可以查询角色
- [ ] `/api/system/permissions` 可以查询权限
- [ ] 管理员账号拥有业务管理权限
- [ ] 无权限账号访问管理接口返回 403

## 4. 用户与门店

- [ ] 用户列表可以分页查询
- [ ] 用户可以新增
- [ ] 用户可以修改
- [ ] 用户状态可以修改
- [ ] 用户档案可以维护
- [ ] 省份列表可以查询
- [ ] 门店可以新增、修改、删除
- [ ] 用户可以绑定门店

## 5. 课程模块

- [ ] 课程分类可以新增、修改、删除
- [ ] 课程可以新增、修改、删除
- [ ] 课程列表支持分页查询
- [ ] 登录用户可以收藏课程
- [ ] 登录用户可以取消收藏课程
- [ ] 登录用户可以查看课程收藏

## 6. 动作库模块

- [ ] 动作分类可以新增、修改、删除
- [ ] 动作可以新增、修改、删除
- [ ] 动作列表支持分页查询
- [ ] 登录用户可以收藏动作
- [ ] 登录用户可以取消收藏动作
- [ ] 登录用户可以查看动作收藏

## 7. 活动首页模块

- [ ] Slogan 可以新增、修改、删除
- [ ] Slogan 可以按状态查询
- [ ] 活动可以新增、修改、删除
- [ ] 活动列表支持分页查询
- [ ] 活动列表支持状态和日期筛选
- [ ] `/api/home` 不登录也可以访问
- [ ] `/api/home` 返回启用状态的 Slogan
- [ ] `/api/home` 返回活动列表摘要

## 8. 统一响应与错误

- [ ] 成功响应包含 `code`
- [ ] 成功响应包含 `message`
- [ ] 成功响应包含 `data`
- [ ] 参数错误返回 422
- [ ] 未登录返回 401
- [ ] 无权限返回 403
- [ ] 业务错误返回统一 JSON 结构

## 9. 后端交付前检查

- [ ] `pytest` 全部通过
- [ ] 已导出 `docs/openapi.json`
- [ ] Swagger 中接口分组清晰
- [ ] 所有接口路径与前端约定一致
- [ ] 管理接口均有权限控制
- [ ] 用户端接口不误用后台权限
```

### 注意事项

1. 这个清单是人工验收用，不参与自动化测试。
2. 每完成一次后端阶段，可以重新勾选一遍。
3. 进入前端阶段前，至少要完成登录、权限、课程、动作、活动首页的验收。

## 十一、步骤 7：运行测试与导出文档

### 运行测试

确保当前目录是：

```text
FastAPI_Vue3_JoyFit/backend
```

执行：

```bash
pytest
```

预期结果：

```text
所有测试通过
```

### 导出 OpenAPI

执行：

```bash
python scripts/export_openapi.py
```

预期生成：

```text
backend/docs/openapi.json
```

### 查看 Swagger

启动服务：

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

访问：

```text
http://127.0.0.1:8000/docs
```

## 十二、本阶段验收清单

| 序号 | 验收项 | 成功标准 |
| --- | --- | --- |
| 1 | OpenAPI 路径测试 | 核心路径全部存在 |
| 2 | 权限种子测试 | 角色和权限完整 |
| 3 | Schema 校验测试 | 非法字段能被拒绝 |
| 4 | 响应结构测试 | `code/message/data` 稳定 |
| 5 | OpenAPI 导出 | 生成 `docs/openapi.json` |
| 6 | 接口验收清单 | `docs/api_checklist.md` 存在 |
| 7 | pytest | 全部测试通过 |
| 8 | Swagger | `/docs` 可访问 |

## 十三、常见问题

### 1. `test_openapi_contains_core_paths` 失败

原因：

通常是新增接口没有注册到 `backend/app/api/router.py`，或测试中的路径和实际接口路径不一致。

修复：

1. 打开 Swagger 查看真实路径。
2. 如果路由漏注册，补充 `api_router.include_router(...)`。
3. 如果接口路径本来就调整了，同步修改 `expected_paths`。

### 2. `test_default_permissions_are_complete` 失败

原因：

`backend/app/db/seed.py` 中缺少某个业务权限。

修复：

1. 对照失败信息中的权限代码。
2. 将缺失权限补到 `DEFAULT_PERMISSIONS`。
3. 重启服务，让种子数据自动补齐到数据库。

### 3. `test_schema_validation` 失败

原因：

Schema 字段范围被改动，或者测试用例和当前业务规则不一致。

修复：

1. 如果业务规则没变，修复 Schema。
2. 如果业务规则已经调整，修改测试期望。

### 4. `python scripts/export_openapi.py` 提示找不到 `app`

原因：

命令不是在 `backend` 目录下执行。

修复：

先进入：

```bash
cd backend
```

再执行：

```bash
python scripts/export_openapi.py
```

### 5. 导出的 OpenAPI 没有最新接口

原因：

路由没有注册，或者导出前代码没有保存。

修复：

1. 确认对应路由文件已经被 `backend/app/api/router.py` 引入。
2. 重新执行导出脚本。
3. 打开 `docs/openapi.json` 搜索接口路径。

## 十四、进入下一阶段的条件

满足以下条件后，可以进入第九阶段：

```text
09_前端基础工程构建.md
```

必须满足：

1. `pytest` 全部通过。
2. `docs/openapi.json` 可以生成。
3. `docs/api_checklist.md` 已创建。
4. Swagger 能正常访问。
5. 后端核心接口已经人工验收。
6. 后端接口路径和响应结构已稳定。

## 十五、问题记录与修复方案

后续你在第八阶段实践中遇到问题，可以直接把错误信息发给我。

我会做两件事：

1. 判断错误属于测试用例、权限种子、Schema 校验、OpenAPI 导出还是接口验收问题。
2. 将修复办法追加到本文件底部。

### 问题追加格式

```text
### 问题 N：问题标题

#### 报错现象

这里记录你遇到的报错信息。

#### 原因分析

这里说明为什么会报错。

#### 修复办法

这里给出具体修复步骤。

#### 需要修改的文件

- 相对路径：xxx
- 修改说明：xxx
- 完整代码或补丁：xxx
```

### 问题 1：执行 `python3 scripts/export_openapi.py` 提示找不到 `app`

#### 报错现象

在 `backend` 目录下执行：

```bash
python3 scripts/export_openapi.py
```

报错：

```text
ModuleNotFoundError: No module named 'app'
```

#### 原因分析

当使用下面这种方式直接运行脚本时：

```bash
python3 scripts/export_openapi.py
```

Python 会优先把脚本所在目录 `backend/scripts` 放进模块搜索路径，而不是稳定地把 `backend` 根目录放进去。

所以脚本中的：

```python
from app.main import app
```

会找不到 `backend/app` 这个包。

#### 修复办法

在脚本开头手动把 `backend` 根目录加入 `sys.path`。

#### 需要修改的文件

- 相对路径：`backend/scripts/export_openapi.py`
- 修改说明：导入 `app.main` 前，先把项目后端根目录加入 Python 模块搜索路径
- 完整代码：

```python
import json
import sys
from pathlib import Path


BACKEND_DIR = Path(__file__).resolve().parents[1]
if str(BACKEND_DIR) not in sys.path:
    sys.path.insert(0, str(BACKEND_DIR))

from app.main import app


OUTPUT_PATH = BACKEND_DIR / "docs" / "openapi.json"


def main() -> None:
    schema = app.openapi()
    OUTPUT_PATH.parent.mkdir(parents=True, exist_ok=True)
    OUTPUT_PATH.write_text(
        json.dumps(schema, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    print(f"OpenAPI schema exported to {OUTPUT_PATH}")


if __name__ == "__main__":
    main()
```

#### 处理完后再验证

确保当前目录是：

```text
FastAPI_Vue3_JoyFit/backend
```

再次执行：

```bash
python3 scripts/export_openapi.py
```

预期结果：

```text
OpenAPI schema exported to /Users/.../FastAPI_Vue3_JoyFit/backend/docs/openapi.json
```
