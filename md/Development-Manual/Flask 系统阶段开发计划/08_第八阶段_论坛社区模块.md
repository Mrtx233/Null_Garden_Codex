# 第 8 阶段：论坛社区模块

本阶段基于第 7 阶段首页和活动模块继续开发，实现帖子列表、发布帖子、帖子详情、评论、二级评论、点赞功能，以及后台管理帖子和评论。

完成本阶段后：

- 前台用户可以浏览帖子。
- 登录用户可以发布帖子、发表评论和二级评论。
- 登录用户可以点赞或取消点赞帖子。
- 后台可以管理帖子状态和评论状态。

---

## 1. 本阶段新增和修改文件

| 类型 | 文件 |
| --- | --- |
| 修改 | `app/blueprints/admin/__init__.py` |
| 新增 | `app/blueprints/admin/forum_routes.py` |
| 修改 | `app/blueprints/web/routes.py` |
| 新增 | `app/templates/admin/forum_posts.html` |
| 新增 | `app/templates/admin/forum_comments.html` |
| 新增 | `app/templates/web/forum_posts.html` |
| 新增 | `app/templates/web/forum_post_form.html` |
| 新增 | `app/templates/web/forum_post_detail.html` |
| 修改 | `app/templates/web/layout.html` |

---

## 2. 注册论坛后台路由

修改文件：`app/blueprints/admin/__init__.py`

```python
from flask import Blueprint


admin_bp = Blueprint("admin", __name__)

from . import routes
from . import rbac_routes
from . import user_routes
from . import store_routes
from . import course_routes
from . import action_routes
from . import activity_routes
from . import forum_routes
```

---

## 3. 论坛后台管理路由

文件：`app/blueprints/admin/forum_routes.py`

```python
from flask import flash, redirect, render_template, url_for

from app.extensions import db
from app.models import ForumComment, ForumPost
from app.utils.auth import permission_required
from . import admin_bp


@admin_bp.route("/forum/posts")
@permission_required("forum:manage")
def forum_posts():
    posts = ForumPost.query.order_by(ForumPost.post_id.desc()).all()
    return render_template("admin/forum_posts.html", posts=posts)


@admin_bp.route("/forum/posts/<int:post_id>/toggle")
@permission_required("forum:manage")
def forum_post_toggle(post_id):
    post = ForumPost.query.get_or_404(post_id)
    post.status = 0 if post.status == 1 else 1
    db.session.commit()
    flash("帖子状态已更新。", "success")
    return redirect(url_for("admin.forum_posts"))


@admin_bp.route("/forum/comments")
@permission_required("forum:manage")
def forum_comments():
    comments = ForumComment.query.order_by(ForumComment.comment_id.desc()).all()
    return render_template("admin/forum_comments.html", comments=comments)


@admin_bp.route("/forum/comments/<int:comment_id>/toggle")
@permission_required("forum:manage")
def forum_comment_toggle(comment_id):
    comment = ForumComment.query.get_or_404(comment_id)
    comment.status = 0 if comment.status == 1 else 1
    db.session.commit()
    flash("评论状态已更新。", "success")
    return redirect(url_for("admin.forum_comments"))
```

---

## 4. 前台论坛路由

在第 7 阶段 `app/blueprints/web/routes.py` 的基础上继续增加以下路由和导入。

```python
from datetime import datetime

from flask import request

from app.models import ForumComment, ForumLike, ForumPost


@web_bp.route("/forum")
def forum_post_list():
    posts = ForumPost.query.filter_by(status=1).order_by(ForumPost.post_id.desc()).all()
    return render_template("web/forum_posts.html", posts=posts)


@web_bp.route("/forum/create", methods=["GET", "POST"])
@login_required
def forum_post_create():
    user = get_current_user()
    if request.method == "POST":
        post = ForumPost(
            user_id=user.user_id,
            title=request.form.get("title", "").strip(),
            content=request.form.get("content", "").strip(),
            created_at=datetime.utcnow(),
            status=1,
        )
        db.session.add(post)
        db.session.commit()
        flash("帖子已发布。", "success")
        return redirect(url_for("web.forum_post_detail", post_id=post.post_id))
    return render_template("web/forum_post_form.html")


@web_bp.route("/forum/<int:post_id>")
def forum_post_detail(post_id):
    post = ForumPost.query.get_or_404(post_id)
    comments = (
        ForumComment.query
        .filter_by(post_id=post.post_id, parent_id=None, status=1)
        .order_by(ForumComment.comment_id.asc())
        .all()
    )
    user = get_current_user()
    liked = False
    if user:
        liked = ForumLike.query.filter_by(post_id=post.post_id, user_id=user.user_id).first() is not None
    return render_template("web/forum_post_detail.html", post=post, comments=comments, liked=liked)


@web_bp.route("/forum/<int:post_id>/comments", methods=["POST"])
@login_required
def forum_comment_create(post_id):
    user = get_current_user()
    post = ForumPost.query.get_or_404(post_id)
    comment = ForumComment(
        post_id=post.post_id,
        user_id=user.user_id,
        parent_id=request.form.get("parent_id") or None,
        content=request.form.get("content", "").strip(),
        created_at=datetime.utcnow(),
        status=1,
    )
    db.session.add(comment)
    post.comment_count = (post.comment_count or 0) + 1
    db.session.commit()
    flash("评论已发布。", "success")
    return redirect(url_for("web.forum_post_detail", post_id=post.post_id))


@web_bp.route("/forum/<int:post_id>/like", methods=["POST"])
@login_required
def forum_post_like(post_id):
    user = get_current_user()
    post = ForumPost.query.get_or_404(post_id)
    like = ForumLike.query.filter_by(post_id=post.post_id, user_id=user.user_id).first()
    if like:
        db.session.delete(like)
        post.like_count = max((post.like_count or 0) - 1, 0)
        flash("已取消点赞。", "success")
    else:
        db.session.add(ForumLike(post_id=post.post_id, user_id=user.user_id, created_at=datetime.utcnow()))
        post.like_count = (post.like_count or 0) + 1
        flash("已点赞。", "success")
    db.session.commit()
    return redirect(url_for("web.forum_post_detail", post_id=post.post_id))
```

---

## 5. 后台论坛模板

文件：`app/templates/admin/forum_posts.html`

```html
{% extends "admin/layout.html" %}
{% block title %}帖子管理{% endblock %}
{% block page_name %}帖子管理{% endblock %}
{% block content %}
<table class="data-table">
    <thead><tr><th>ID</th><th>标题</th><th>作者</th><th>点赞</th><th>评论</th><th>状态</th><th>操作</th></tr></thead>
    <tbody>
        {% for post in posts %}
            <tr>
                <td>{{ post.post_id }}</td>
                <td>{{ post.title }}</td>
                <td>{{ post.user.username if post.user else "-" }}</td>
                <td>{{ post.like_count }}</td>
                <td>{{ post.comment_count }}</td>
                <td>{{ "显示" if post.status == 1 else "隐藏" }}</td>
                <td><a href="{{ url_for('admin.forum_post_toggle', post_id=post.post_id) }}">切换状态</a></td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
```

文件：`app/templates/admin/forum_comments.html`

```html
{% extends "admin/layout.html" %}
{% block title %}评论管理{% endblock %}
{% block page_name %}评论管理{% endblock %}
{% block content %}
<table class="data-table">
    <thead><tr><th>ID</th><th>帖子</th><th>用户</th><th>内容</th><th>状态</th><th>操作</th></tr></thead>
    <tbody>
        {% for comment in comments %}
            <tr>
                <td>{{ comment.comment_id }}</td>
                <td>{{ comment.post.title if comment.post else "-" }}</td>
                <td>{{ comment.user.username if comment.user else "-" }}</td>
                <td>{{ comment.content }}</td>
                <td>{{ "显示" if comment.status == 1 else "隐藏" }}</td>
                <td><a href="{{ url_for('admin.forum_comment_toggle', comment_id=comment.comment_id) }}">切换状态</a></td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
```

---

## 6. 前台论坛模板

文件：`app/templates/web/forum_posts.html`

```html
{% extends "web/layout.html" %}
{% block title %}社区论坛{% endblock %}
{% block content %}
<section class="content-section">
    <div class="toolbar">
        <h1>社区论坛</h1>
        <a class="button" href="{{ url_for('web.forum_post_create') }}">发布帖子</a>
    </div>
    <div class="card-grid">
        {% for post in posts %}
            <a class="content-card" href="{{ url_for('web.forum_post_detail', post_id=post.post_id) }}">
                <strong>{{ post.title }}</strong>
                <span>{{ post.user.username if post.user else "匿名" }} · {{ post.created_at }}</span>
                <p>{{ post.like_count }} 点赞 · {{ post.comment_count }} 评论</p>
            </a>
        {% endfor %}
    </div>
</section>
{% endblock %}
```

文件：`app/templates/web/forum_post_form.html`

```html
{% extends "web/layout.html" %}
{% block title %}发布帖子{% endblock %}
{% block content %}
<section class="content-section">
    <h1>发布帖子</h1>
    <form class="admin-form" method="post">
        <label>标题 <input type="text" name="title" required></label>
        <label>内容 <textarea name="content" required></textarea></label>
        <button type="submit">发布</button>
    </form>
</section>
{% endblock %}
```

文件：`app/templates/web/forum_post_detail.html`

```html
{% extends "web/layout.html" %}
{% block title %}{{ post.title }}{% endblock %}
{% block content %}
<section class="content-section">
    <h1>{{ post.title }}</h1>
    <p>{{ post.user.username if post.user else "匿名" }} · {{ post.created_at }}</p>
    <article>{{ post.content }}</article>

    <form method="post" action="{{ url_for('web.forum_post_like', post_id=post.post_id) }}">
        <button class="button" type="submit">{{ "取消点赞" if liked else "点赞" }}（{{ post.like_count }}）</button>
    </form>

    <h2>评论</h2>
    <form class="admin-form" method="post" action="{{ url_for('web.forum_comment_create', post_id=post.post_id) }}">
        <label>发表评论 <textarea name="content" required></textarea></label>
        <button type="submit">提交评论</button>
    </form>

    <div class="comment-list">
        {% for comment in comments %}
            <article class="comment-item">
                <strong>{{ comment.user.username if comment.user else "匿名" }}</strong>
                <p>{{ comment.content }}</p>
                <form method="post" action="{{ url_for('web.forum_comment_create', post_id=post.post_id) }}">
                    <input type="hidden" name="parent_id" value="{{ comment.comment_id }}">
                    <textarea name="content" placeholder="回复这条评论" required></textarea>
                    <button type="submit">回复</button>
                </form>

                {% for child in comment.children %}
                    {% if child.status == 1 %}
                        <div class="comment-child">
                            <strong>{{ child.user.username if child.user else "匿名" }}</strong>
                            <p>{{ child.content }}</p>
                        </div>
                    {% endif %}
                {% endfor %}
            </article>
        {% endfor %}
    </div>
</section>
{% endblock %}
```

---

## 7. 前台导航和样式补充

修改文件：`app/templates/web/layout.html` 的导航。

```html
<nav class="site-nav">
    <a href="{{ url_for('web.index') }}">首页</a>
    <a href="{{ url_for('web.course_list') }}">课程</a>
    <a href="{{ url_for('web.action_list') }}">动作库</a>
    <a href="{{ url_for('web.activity_list') }}">活动</a>
    <a href="{{ url_for('web.forum_post_list') }}">社区</a>
    {% if current_user %}
        <a href="{{ url_for('auth.logout') }}">退出</a>
    {% else %}
        <a href="{{ url_for('auth.login') }}">登录</a>
    {% endif %}
</nav>
```

修改文件：`app/static/css/web.css`

```css
.comment-list {
    display: grid;
    gap: 16px;
    margin-top: 24px;
}

.comment-item,
.comment-child {
    padding: 16px;
    background: #ffffff;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
}

.comment-child {
    margin-top: 12px;
    margin-left: 24px;
    background: #f9fafb;
}

.comment-item textarea {
    width: 100%;
    min-height: 72px;
    margin-top: 8px;
    padding: 10px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
}
```

---

## 8. 后台菜单

修改文件：`app/templates/admin/layout.html` 的菜单。

```html
{% if has_permission("forum:manage") %}
    <a href="{{ url_for('admin.forum_posts') }}">帖子管理</a>
    <a href="{{ url_for('admin.forum_comments') }}">评论管理</a>
{% endif %}
```

---

## 9. 本阶段验证

访问：

```text
前台论坛：http://127.0.0.1:5000/forum
发布帖子：http://127.0.0.1:5000/forum/create
帖子管理：http://127.0.0.1:5000/admin/forum/posts
评论管理：http://127.0.0.1:5000/admin/forum/comments
```

验收标准：

| 检查项 | 预期结果 |
| --- | --- |
| 帖子列表 | 展示状态为显示的帖子 |
| 发布帖子 | 登录后可以发布 |
| 帖子详情 | 展示内容、点赞数、评论数 |
| 评论 | 登录后可以评论 |
| 二级评论 | 可以回复一级评论 |
| 点赞 | 可以点赞和取消点赞 |
| 后台帖子管理 | 可以隐藏/显示帖子 |
| 后台评论管理 | 可以隐藏/显示评论 |

---

## 10. 全项目最终串联

完成 1 到 8 阶段后，系统模块关系如下：

```text
项目基础搭建
  ↓
登录认证
  ↓
RBAC 权限
  ↓
用户与门店
  ↓
课程业务
  ↓
动作库业务
  ↓
活动与首页配置
  ↓
论坛社区
```

最终访问路径：

```text
前台首页：http://127.0.0.1:5000/
课程列表：http://127.0.0.1:5000/courses
动作库：http://127.0.0.1:5000/actions
活动列表：http://127.0.0.1:5000/activities
论坛社区：http://127.0.0.1:5000/forum
后台首页：http://127.0.0.1:5000/admin/
```

