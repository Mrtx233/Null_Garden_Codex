# 第 7 阶段：活动与首页配置模块

本阶段基于第 6 阶段动作库业务继续开发，实现 Slogan 管理、赛事活动管理、首页 Banner / Slogan 展示、活动列表和活动详情。

完成本阶段后：

- 后台可以维护 Slogan。
- 后台可以维护赛事活动。
- 前台首页展示启用 Slogan、课程、动作和活动。
- 前台可以查看活动列表和活动详情。

---

## 1. 本阶段新增和修改文件

| 类型 | 文件 |
| --- | --- |
| 修改 | `app/blueprints/admin/__init__.py` |
| 新增 | `app/blueprints/admin/activity_routes.py` |
| 修改 | `app/blueprints/web/routes.py` |
| 新增 | `app/templates/admin/slogans.html` |
| 新增 | `app/templates/admin/slogan_form.html` |
| 新增 | `app/templates/admin/activities.html` |
| 新增 | `app/templates/admin/activity_form.html` |
| 修改 | `app/templates/web/index.html` |
| 新增 | `app/templates/web/activities.html` |
| 新增 | `app/templates/web/activity_detail.html` |

---

## 2. 注册活动后台路由

修改文件：`app/blueprints/admin/__init__.py`

```python
from flask import Blueprint


admin_bp = Blueprint("admin", __name__)

from . import routes
from . import rbac_routes
from . import user_routes
from . import store_routes
from . import course_routes
from . import action_routes
from . import activity_routes
```

---

## 3. Slogan 和活动后台路由

文件：`app/blueprints/admin/activity_routes.py`

```python
from flask import flash, redirect, render_template, request, url_for

from app.extensions import db
from app.models import ActivityEvent, SloganInfo
from app.utils.auth import permission_required
from . import admin_bp


@admin_bp.route("/slogans")
@permission_required("activity:manage")
def slogans():
    items = SloganInfo.query.order_by(SloganInfo.slogan_id.desc()).all()
    return render_template("admin/slogans.html", slogans=items)


@admin_bp.route("/slogans/create", methods=["GET", "POST"])
@permission_required("activity:manage")
def slogan_create():
    if request.method == "POST":
        item = SloganInfo(
            slogan_name=request.form.get("slogan_name", "").strip(),
            slogan_content=request.form.get("slogan_content", "").strip(),
            slogan_image_url=request.form.get("slogan_image_url", "").strip(),
            status=int(request.form.get("status", 1)),
        )
        db.session.add(item)
        db.session.commit()
        flash("Slogan 已创建。", "success")
        return redirect(url_for("admin.slogans"))
    return render_template("admin/slogan_form.html", slogan=None)


@admin_bp.route("/slogans/<int:slogan_id>/edit", methods=["GET", "POST"])
@permission_required("activity:manage")
def slogan_edit(slogan_id):
    item = SloganInfo.query.get_or_404(slogan_id)
    if request.method == "POST":
        item.slogan_name = request.form.get("slogan_name", "").strip()
        item.slogan_content = request.form.get("slogan_content", "").strip()
        item.slogan_image_url = request.form.get("slogan_image_url", "").strip()
        item.status = int(request.form.get("status", 1))
        db.session.commit()
        flash("Slogan 已更新。", "success")
        return redirect(url_for("admin.slogans"))
    return render_template("admin/slogan_form.html", slogan=item)


@admin_bp.route("/activities")
@permission_required("activity:manage")
def activities():
    items = ActivityEvent.query.order_by(ActivityEvent.event_date.desc()).all()
    return render_template("admin/activities.html", activities=items)


@admin_bp.route("/activities/create", methods=["GET", "POST"])
@permission_required("activity:manage")
def activity_create():
    if request.method == "POST":
        item = ActivityEvent(
            title=request.form.get("title", "").strip(),
            event_date=request.form["event_date"],
            location=request.form.get("location", "").strip(),
            status=request.form.get("status", "预告").strip(),
            description=request.form.get("description", "").strip(),
            tags=request.form.get("tags", "").strip(),
            prize=request.form.get("prize", "").strip(),
            scale=request.form.get("scale", "").strip(),
        )
        db.session.add(item)
        db.session.commit()
        flash("活动已创建。", "success")
        return redirect(url_for("admin.activities"))
    return render_template("admin/activity_form.html", activity=None)


@admin_bp.route("/activities/<int:event_id>/edit", methods=["GET", "POST"])
@permission_required("activity:manage")
def activity_edit(event_id):
    item = ActivityEvent.query.get_or_404(event_id)
    if request.method == "POST":
        item.title = request.form.get("title", "").strip()
        item.event_date = request.form["event_date"]
        item.location = request.form.get("location", "").strip()
        item.status = request.form.get("status", "预告").strip()
        item.description = request.form.get("description", "").strip()
        item.tags = request.form.get("tags", "").strip()
        item.prize = request.form.get("prize", "").strip()
        item.scale = request.form.get("scale", "").strip()
        db.session.commit()
        flash("活动已更新。", "success")
        return redirect(url_for("admin.activities"))
    return render_template("admin/activity_form.html", activity=item)
```

---

## 4. 前台首页和活动路由

在第 6 阶段 `app/blueprints/web/routes.py` 的基础上整理为以下完整版本。

```python
from flask import flash, redirect, render_template, url_for

from app.extensions import db
from app.models import (
    Action,
    ActionCategory,
    ActivityEvent,
    Course,
    CourseCategory,
    SloganInfo,
    UserActionFavorite,
    UserCourseFavorite,
)
from app.utils.auth import get_current_user, login_required
from . import web_bp


@web_bp.route("/")
def index():
    slogans = SloganInfo.query.filter_by(status=1).order_by(SloganInfo.slogan_id.desc()).limit(3).all()
    courses = Course.query.filter_by(status=1).order_by(Course.course_id.desc()).limit(6).all()
    actions = Action.query.order_by(Action.action_id.desc()).limit(6).all()
    activities = ActivityEvent.query.order_by(ActivityEvent.event_date.desc()).limit(4).all()
    return render_template(
        "web/index.html",
        slogans=slogans,
        courses=courses,
        actions=actions,
        activities=activities,
    )


@web_bp.route("/activities")
def activity_list():
    activities = ActivityEvent.query.order_by(ActivityEvent.event_date.desc()).all()
    return render_template("web/activities.html", activities=activities)


@web_bp.route("/activities/<int:event_id>")
def activity_detail(event_id):
    activity = ActivityEvent.query.get_or_404(event_id)
    return render_template("web/activity_detail.html", activity=activity)


@web_bp.route("/courses")
def course_list():
    categories = CourseCategory.query.filter_by(status=1).order_by(CourseCategory.category_id.asc()).all()
    courses = Course.query.filter_by(status=1).order_by(Course.course_id.desc()).all()
    return render_template("web/courses.html", categories=categories, courses=courses)


@web_bp.route("/courses/<int:course_id>")
def course_detail(course_id):
    course = Course.query.get_or_404(course_id)
    user = get_current_user()
    is_favorite = False
    if user:
        is_favorite = UserCourseFavorite.query.filter_by(user_id=user.user_id, course_id=course.course_id).first() is not None
    return render_template("web/course_detail.html", course=course, is_favorite=is_favorite)


@web_bp.route("/courses/<int:course_id>/favorite", methods=["POST"])
@login_required
def course_favorite(course_id):
    user = get_current_user()
    favorite = UserCourseFavorite.query.filter_by(user_id=user.user_id, course_id=course_id).first()
    if favorite:
        db.session.delete(favorite)
        flash("已取消收藏课程。", "success")
    else:
        db.session.add(UserCourseFavorite(user_id=user.user_id, course_id=course_id))
        flash("已收藏课程。", "success")
    db.session.commit()
    return redirect(url_for("web.course_detail", course_id=course_id))


@web_bp.route("/actions")
def action_list():
    categories = ActionCategory.query.order_by(ActionCategory.category_id.asc()).all()
    actions = Action.query.order_by(Action.action_id.desc()).all()
    return render_template("web/actions.html", categories=categories, actions=actions)


@web_bp.route("/actions/<int:action_id>")
def action_detail(action_id):
    action = Action.query.get_or_404(action_id)
    user = get_current_user()
    is_favorite = False
    if user:
        is_favorite = UserActionFavorite.query.filter_by(user_id=user.user_id, action_id=action.action_id).first() is not None
    return render_template("web/action_detail.html", action=action, is_favorite=is_favorite)


@web_bp.route("/actions/<int:action_id>/favorite", methods=["POST"])
@login_required
def action_favorite(action_id):
    user = get_current_user()
    favorite = UserActionFavorite.query.filter_by(user_id=user.user_id, action_id=action_id).first()
    if favorite:
        db.session.delete(favorite)
        flash("已取消收藏动作。", "success")
    else:
        db.session.add(UserActionFavorite(user_id=user.user_id, action_id=action_id))
        flash("已收藏动作。", "success")
    db.session.commit()
    return redirect(url_for("web.action_detail", action_id=action_id))
```

---

## 5. 后台模板

文件：`app/templates/admin/slogans.html`

```html
{% extends "admin/layout.html" %}
{% block title %}Slogan 管理{% endblock %}
{% block page_name %}Slogan 管理{% endblock %}
{% block content %}
<div class="toolbar"><a class="button" href="{{ url_for('admin.slogan_create') }}">新增 Slogan</a></div>
<table class="data-table">
    <thead><tr><th>ID</th><th>名称</th><th>内容</th><th>状态</th><th>操作</th></tr></thead>
    <tbody>{% for item in slogans %}<tr><td>{{ item.slogan_id }}</td><td>{{ item.slogan_name }}</td><td>{{ item.slogan_content }}</td><td>{{ "启用" if item.status == 1 else "禁用" }}</td><td><a href="{{ url_for('admin.slogan_edit', slogan_id=item.slogan_id) }}">编辑</a></td></tr>{% endfor %}</tbody>
</table>
{% endblock %}
```

文件：`app/templates/admin/slogan_form.html`

```html
{% extends "admin/layout.html" %}
{% block title %}{{ "编辑 Slogan" if slogan else "新增 Slogan" }}{% endblock %}
{% block page_name %}{{ "编辑 Slogan" if slogan else "新增 Slogan" }}{% endblock %}
{% block content %}
<form class="admin-form" method="post">
    <label>名称 <input type="text" name="slogan_name" value="{{ slogan.slogan_name if slogan else '' }}" required></label>
    <label>内容 <input type="text" name="slogan_content" value="{{ slogan.slogan_content if slogan else '' }}" required></label>
    <label>图片 URL <input type="text" name="slogan_image_url" value="{{ slogan.slogan_image_url if slogan else '' }}"></label>
    <label>状态 <select name="status"><option value="1">启用</option><option value="0" {% if slogan and slogan.status == 0 %}selected{% endif %}>禁用</option></select></label>
    <button type="submit">保存</button>
</form>
{% endblock %}
```

文件：`app/templates/admin/activities.html`

```html
{% extends "admin/layout.html" %}
{% block title %}赛事活动{% endblock %}
{% block page_name %}赛事活动{% endblock %}
{% block content %}
<div class="toolbar"><a class="button" href="{{ url_for('admin.activity_create') }}">新增活动</a></div>
<table class="data-table">
    <thead><tr><th>ID</th><th>标题</th><th>日期</th><th>地点</th><th>状态</th><th>操作</th></tr></thead>
    <tbody>{% for item in activities %}<tr><td>{{ item.event_id }}</td><td>{{ item.title }}</td><td>{{ item.event_date }}</td><td>{{ item.location }}</td><td>{{ item.status }}</td><td><a href="{{ url_for('admin.activity_edit', event_id=item.event_id) }}">编辑</a></td></tr>{% endfor %}</tbody>
</table>
{% endblock %}
```

文件：`app/templates/admin/activity_form.html`

```html
{% extends "admin/layout.html" %}
{% block title %}{{ "编辑活动" if activity else "新增活动" }}{% endblock %}
{% block page_name %}{{ "编辑活动" if activity else "新增活动" }}{% endblock %}
{% block content %}
<form class="admin-form" method="post">
    <label>活动标题 <input type="text" name="title" value="{{ activity.title if activity else '' }}" required></label>
    <label>活动日期 <input type="date" name="event_date" value="{{ activity.event_date if activity else '' }}" required></label>
    <label>活动地点 <input type="text" name="location" value="{{ activity.location if activity else '' }}" required></label>
    <label>状态 <input type="text" name="status" value="{{ activity.status if activity else '预告' }}" required></label>
    <label>标签 <input type="text" name="tags" value="{{ activity.tags if activity else '' }}"></label>
    <label>奖金/奖品 <input type="text" name="prize" value="{{ activity.prize if activity else '' }}"></label>
    <label>规模 <input type="text" name="scale" value="{{ activity.scale if activity else '' }}"></label>
    <label>活动描述 <textarea name="description" required>{{ activity.description if activity else '' }}</textarea></label>
    <button type="submit">保存</button>
</form>
{% endblock %}
```

---

## 6. 前台首页模板

修改文件：`app/templates/web/index.html`

```html
{% extends "web/layout.html" %}

{% block title %}JoyFit 首页{% endblock %}

{% block content %}
<section class="hero">
    <div>
        <p class="eyebrow">JoyFit Training</p>
        {% if slogans %}
            <h1>{{ slogans[0].slogan_content }}</h1>
        {% else %}
            <h1>训练、课程、门店和社区的一体化健身平台</h1>
        {% endif %}
        <p class="hero-text">发现课程、学习动作、参与活动，并和社区成员一起持续训练。</p>
    </div>
</section>

<section class="content-section">
    <h2>热门课程</h2>
    <div class="card-grid">
        {% for course in courses %}
            <a class="content-card" href="{{ url_for('web.course_detail', course_id=course.course_id) }}">
                <strong>{{ course.course_name }}</strong>
                <span>{{ course.category.category_name if course.category else "未分类" }}</span>
            </a>
        {% endfor %}
    </div>
</section>

<section class="content-section">
    <h2>动作库</h2>
    <div class="card-grid">
        {% for action in actions %}
            <a class="content-card" href="{{ url_for('web.action_detail', action_id=action.action_id) }}">
                <strong>{{ action.action_name }}</strong>
                <span>{{ action.category.category_name if action.category else "未分类" }}</span>
            </a>
        {% endfor %}
    </div>
</section>

<section class="content-section">
    <h2>赛事活动</h2>
    <div class="card-grid">
        {% for activity in activities %}
            <a class="content-card" href="{{ url_for('web.activity_detail', event_id=activity.event_id) }}">
                <strong>{{ activity.title }}</strong>
                <span>{{ activity.event_date }} · {{ activity.location }}</span>
                <p>{{ activity.status }}</p>
            </a>
        {% endfor %}
    </div>
</section>
{% endblock %}
```

---

## 7. 前台活动模板

文件：`app/templates/web/activities.html`

```html
{% extends "web/layout.html" %}
{% block title %}赛事活动{% endblock %}
{% block content %}
<section class="content-section">
    <h1>赛事活动</h1>
    <div class="card-grid">
        {% for activity in activities %}
            <a class="content-card" href="{{ url_for('web.activity_detail', event_id=activity.event_id) }}">
                <strong>{{ activity.title }}</strong>
                <span>{{ activity.event_date }} · {{ activity.location }}</span>
                <p>{{ activity.status }}</p>
            </a>
        {% endfor %}
    </div>
</section>
{% endblock %}
```

文件：`app/templates/web/activity_detail.html`

```html
{% extends "web/layout.html" %}
{% block title %}{{ activity.title }}{% endblock %}
{% block content %}
<section class="content-section">
    <h1>{{ activity.title }}</h1>
    <p>{{ activity.event_date }} · {{ activity.location }} · {{ activity.status }}</p>
    <p>标签：{{ activity.tags or "-" }}</p>
    <p>奖品：{{ activity.prize or "-" }}</p>
    <p>规模：{{ activity.scale or "-" }}</p>
    <article>{{ activity.description }}</article>
</section>
{% endblock %}
```

---

## 8. 后台菜单

修改文件：`app/templates/admin/layout.html` 的菜单。

```html
{% if has_permission("activity:manage") %}
    <a href="{{ url_for('admin.slogans') }}">Slogan 管理</a>
    <a href="{{ url_for('admin.activities') }}">赛事活动</a>
{% endif %}
```

---

## 9. 本阶段验证

访问：

```text
Slogan 管理：http://127.0.0.1:5000/admin/slogans
赛事活动后台：http://127.0.0.1:5000/admin/activities
前台首页：http://127.0.0.1:5000/
活动列表：http://127.0.0.1:5000/activities
```

验收标准：

| 检查项 | 预期结果 |
| --- | --- |
| Slogan 管理 | 可以新增和编辑 |
| 活动管理 | 可以新增和编辑 |
| 首页展示 | 展示启用 Slogan、课程、动作、活动 |
| 活动列表 | 展示所有活动 |
| 活动详情 | 展示活动完整信息 |

---

## 10. 下一阶段衔接

第 8 阶段将继续开发论坛社区模块：

- 帖子列表。
- 发布帖子。
- 帖子详情。
- 一级评论和二级评论。
- 点赞功能。
- 后台管理帖子和评论。

