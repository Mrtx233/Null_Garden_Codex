# 第 1 阶段：项目基础搭建

本阶段目标：搭建 `flask_joyfit_fullstack` 的 Flask 项目骨架，完成数据库连接、SQLAlchemy 扩展、应用工厂、基础蓝图、前后台基础模板、静态资源目录和全部数据模型。

完成后项目可以正常启动，并能通过 Flask-Migrate 生成数据库迁移。

---

## 1. 本阶段任务清单

| 序号 | 任务 | 说明 |
| --- | --- | --- |
| 1 | 创建项目目录 | 按 Flask 应用工厂结构组织代码 |
| 2 | 安装依赖 | Flask、Flask-SQLAlchemy、Flask-Migrate、PyMySQL、python-dotenv |
| 3 | 编写配置文件 | 支持开发、测试、生产环境 |
| 4 | 初始化扩展 | 创建 `db`、`migrate` |
| 5 | 创建应用工厂 | 统一注册配置、扩展、蓝图、错误页 |
| 6 | 创建基础蓝图 | 后台 `admin`、前台 `web`、认证 `auth` |
| 7 | 创建基础模板 | 后台首页、前台首页、登录页、错误页 |
| 8 | 创建数据模型 | 根据已有 SQL 补全 20 张表的 SQLAlchemy 模型 |
| 9 | 准备环境变量 | 提供 `.env.example` |
| 10 | 验证启动 | 启动项目、检查数据库连接、生成迁移文件 |

---

## 2. 项目目录结构

```text
flask_joyfit_fullstack/
├─ app/
│  ├─ __init__.py
│  ├─ extensions.py
│  ├─ blueprints/
│  │  ├─ __init__.py
│  │  ├─ admin/
│  │  │  ├─ __init__.py
│  │  │  └─ routes.py
│  │  ├─ auth/
│  │  │  ├─ __init__.py
│  │  │  └─ routes.py
│  │  └─ web/
│  │     ├─ __init__.py
│  │     └─ routes.py
│  ├─ models/
│  │  ├─ __init__.py
│  │  ├─ action.py
│  │  ├─ activity.py
│  │  ├─ course.py
│  │  ├─ forum.py
│  │  ├─ store.py
│  │  └─ system.py
│  ├─ static/
│  │  ├─ css/
│  │  │  ├─ admin.css
│  │  │  └─ web.css
│  │  ├─ images/
│  │  └─ js/
│  └─ templates/
│     ├─ errors/
│     │  ├─ 404.html
│     │  └─ 500.html
│     ├─ admin/
│     │  ├─ dashboard.html
│     │  └─ layout.html
│     ├─ auth/
│     │  └─ login.html
│     └─ web/
│        ├─ index.html
│        └─ layout.html
├─ migrations/
├─ .env.example
├─ config.py
├─ requirements.txt
└─ run.py
```

如果是手动创建项目，先在项目根目录执行下面命令，把模板和静态资源目录一次性建好。

macOS / Linux：

```bash
mkdir -p app/blueprints/admin app/blueprints/auth app/blueprints/web
mkdir -p app/models app/utils
mkdir -p app/templates/web app/templates/admin app/templates/auth app/templates/errors
mkdir -p app/static/css app/static/js app/static/images
mkdir -p migrations
```

Windows PowerShell：

```powershell
New-Item -ItemType Directory -Force app\blueprints\admin,app\blueprints\auth,app\blueprints\web
New-Item -ItemType Directory -Force app\models,app\utils
New-Item -ItemType Directory -Force app\templates\web,app\templates\admin,app\templates\auth,app\templates\errors
New-Item -ItemType Directory -Force app\static\css,app\static\js,app\static\images
New-Item -ItemType Directory -Force migrations
```

注意：`render_template("web/index.html")` 对应的真实文件路径必须是：

```text
flask_joyfit_fullstack/app/templates/web/index.html
```

---

## 3. 创建虚拟环境并安装依赖

### 3.1 创建虚拟环境

```powershell
python -m venv .venv
.\.venv\Scripts\activate
```

### 3.2 安装依赖

```powershell
pip install Flask Flask-SQLAlchemy Flask-Migrate PyMySQL python-dotenv
```

### 3.3 生成依赖文件

文件：`requirements.txt`

```txt
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
PyMySQL==1.1.1
python-dotenv==1.0.1
```

---

## 4. 环境变量配置

文件：`.env.example`

```env
FLASK_APP=run.py
FLASK_ENV=development
FLASK_DEBUG=1

SECRET_KEY=replace-this-with-a-random-secret-key

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=flask_joyfit_fullstack
DB_CHARSET=utf8mb4
```

实际开发时复制一份：

```powershell
copy .env.example .env
```

然后按本机 MySQL 信息修改 `.env`。

---

## 5. 项目配置文件

文件：`config.py`

```python
import os
from urllib.parse import quote_plus

from dotenv import load_dotenv


load_dotenv()


class BaseConfig:
    SECRET_KEY = os.getenv("SECRET_KEY", "dev-secret-key")

    DB_HOST = os.getenv("DB_HOST", "127.0.0.1")
    DB_PORT = int(os.getenv("DB_PORT", "3306"))
    DB_USER = os.getenv("DB_USER", "root")
    DB_PASSWORD = os.getenv("DB_PASSWORD", "123456")
    DB_NAME = os.getenv("DB_NAME", "flask_joyfit_fullstack")
    DB_CHARSET = os.getenv("DB_CHARSET", "utf8mb4")

    SQLALCHEMY_DATABASE_URI = (
        f"mysql+pymysql://{DB_USER}:{quote_plus(DB_PASSWORD)}"
        f"@{DB_HOST}:{DB_PORT}/{DB_NAME}?charset={DB_CHARSET}"
    )
    SQLALCHEMY_TRACK_MODIFICATIONS = False
    SQLALCHEMY_ENGINE_OPTIONS = {
        "pool_pre_ping": True,
        "pool_recycle": 280,
    }

    JSON_AS_ASCII = False


class DevelopmentConfig(BaseConfig):
    DEBUG = True


class TestingConfig(BaseConfig):
    TESTING = True
    SQLALCHEMY_DATABASE_URI = "sqlite:///:memory:"


class ProductionConfig(BaseConfig):
    DEBUG = False


config_map = {
    "development": DevelopmentConfig,
    "testing": TestingConfig,
    "production": ProductionConfig,
}
```

---

## 6. 扩展初始化

文件：`app/extensions.py`

```python
from flask_migrate import Migrate
from flask_sqlalchemy import SQLAlchemy


db = SQLAlchemy()
migrate = Migrate()
```

---

## 7. 应用工厂

文件：`app/__init__.py`

```python
import os

from flask import Flask, render_template

from config import config_map
from .extensions import db, migrate


def create_app(config_name=None):
    app = Flask(__name__)

    env_name = config_name or os.getenv("FLASK_ENV", "development")
    app.config.from_object(config_map.get(env_name, config_map["development"]))

    register_extensions(app)

    from . import models

    register_blueprints(app)
    register_error_handlers(app)

    return app


def register_extensions(app):
    db.init_app(app)
    migrate.init_app(app, db)


def register_blueprints(app):
    from .blueprints.admin import admin_bp
    from .blueprints.auth import auth_bp
    from .blueprints.web import web_bp

    app.register_blueprint(web_bp)
    app.register_blueprint(auth_bp, url_prefix="/auth")
    app.register_blueprint(admin_bp, url_prefix="/admin")


def register_error_handlers(app):
    @app.errorhandler(404)
    def page_not_found(error):
        return render_template("errors/404.html"), 404

    @app.errorhandler(500)
    def internal_server_error(error):
        return render_template("errors/500.html"), 500
```

---

## 8. 启动入口

文件：`run.py`

```python
from app import create_app


app = create_app()


if __name__ == "__main__":
    app.run(host="127.0.0.1", port=5000, debug=True)
```

---

## 9. 蓝图代码

### 9.1 蓝图包初始化

文件：`app/blueprints/__init__.py`

```python
```

### 9.2 前台蓝图

文件：`app/blueprints/web/__init__.py`

```python
from flask import Blueprint


web_bp = Blueprint("web", __name__)

from . import routes
```

文件：`app/blueprints/web/routes.py`

```python
from flask import render_template

from . import web_bp


@web_bp.route("/")
def index():
    return render_template("web/index.html")
```

### 9.3 后台蓝图

文件：`app/blueprints/admin/__init__.py`

```python
from flask import Blueprint


admin_bp = Blueprint("admin", __name__)

from . import routes
```

文件：`app/blueprints/admin/routes.py`

```python
from flask import render_template

from . import admin_bp


@admin_bp.route("/")
def dashboard():
    return render_template("admin/dashboard.html")
```

### 9.4 认证蓝图

文件：`app/blueprints/auth/__init__.py`

```python
from flask import Blueprint


auth_bp = Blueprint("auth", __name__)

from . import routes
```

文件：`app/blueprints/auth/routes.py`

```python
from flask import render_template

from . import auth_bp


@auth_bp.route("/login", methods=["GET", "POST"])
def login():
    return render_template("auth/login.html")
```

---

## 10. 数据模型

### 10.1 模型统一导入

文件：`app/models/__init__.py`

```python
from .action import Action, ActionCategory, UserActionFavorite
from .activity import ActivityEvent, SloganInfo
from .course import Course, CourseCategory, StoreCourseSchedule, UserCourseFavorite
from .forum import ForumComment, ForumLike, ForumPost
from .store import Store, StoreProvince, UserStore
from .system import Permission, Role, RolePermission, User, UserProfile


__all__ = [
    "Action",
    "ActionCategory",
    "ActivityEvent",
    "Course",
    "CourseCategory",
    "ForumComment",
    "ForumLike",
    "ForumPost",
    "Permission",
    "Role",
    "RolePermission",
    "SloganInfo",
    "Store",
    "StoreCourseSchedule",
    "StoreProvince",
    "User",
    "UserActionFavorite",
    "UserCourseFavorite",
    "UserProfile",
    "UserStore",
]
```

### 10.2 系统、权限、用户模型

文件：`app/models/system.py`

```python
from datetime import datetime

from werkzeug.security import check_password_hash, generate_password_hash

from app.extensions import db


class Permission(db.Model):
    __tablename__ = "sys_permission"

    id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="权限ID")
    permission_code = db.Column(db.String(64), nullable=False, unique=True, comment="权限标识符")
    permission_name = db.Column(db.String(64), nullable=False, comment="权限名称")
    menu_path = db.Column(db.String(255), comment="前端路由路径")

    role_permissions = db.relationship("RolePermission", back_populates="permission")

    def __repr__(self):
        return f"<Permission {self.permission_code}>"


class Role(db.Model):
    __tablename__ = "sys_role"

    id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="角色ID")
    role_code = db.Column(db.String(64), nullable=False, unique=True, comment="角色代码")
    role_name = db.Column(db.String(64), nullable=False, comment="角色名称")

    users = db.relationship("User", back_populates="role")
    role_permissions = db.relationship("RolePermission", back_populates="role", cascade="all, delete-orphan")

    def __repr__(self):
        return f"<Role {self.role_code}>"


class RolePermission(db.Model):
    __tablename__ = "sys_role_permission"

    id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="关联ID")
    role_id = db.Column(db.BigInteger, db.ForeignKey("sys_role.id"), nullable=False, comment="角色ID")
    permission_id = db.Column(db.BigInteger, db.ForeignKey("sys_permission.id"), nullable=False, comment="权限ID")

    role = db.relationship("Role", back_populates="role_permissions")
    permission = db.relationship("Permission", back_populates="role_permissions")


class User(db.Model):
    __tablename__ = "sys_user"

    user_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="用户ID")
    role_id = db.Column(db.BigInteger, db.ForeignKey("sys_role.id"), nullable=False, comment="角色ID")
    username = db.Column(db.String(64), nullable=False, unique=True, comment="用户名")
    password = db.Column(db.String(255), nullable=False, comment="密码")
    real_name = db.Column(db.String(64), comment="真实姓名")
    phone = db.Column(db.String(20), comment="手机号")
    status = db.Column(db.Integer, default=1, comment="状态")

    role = db.relationship("Role", back_populates="users")
    profile = db.relationship("UserProfile", back_populates="user", uselist=False, cascade="all, delete-orphan")

    def set_password(self, raw_password):
        self.password = generate_password_hash(raw_password)

    def check_password(self, raw_password):
        return check_password_hash(self.password, raw_password)

    @property
    def is_active(self):
        return self.status == 1

    def __repr__(self):
        return f"<User {self.username}>"


class UserProfile(db.Model):
    __tablename__ = "sys_user_profile"

    profile_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="档案ID")
    user_id = db.Column(db.BigInteger, db.ForeignKey("sys_user.user_id"), nullable=False, unique=True, comment="用户ID")
    level = db.Column(db.Integer, comment="等级")
    gender = db.Column(db.Integer, comment="性别")
    birthday = db.Column(db.Date, comment="生日")
    height_cm = db.Column(db.Numeric(5, 2), comment="身高(cm)")
    weight_kg = db.Column(db.Numeric(5, 2), comment="体重(kg)")
    avatar_url = db.Column(db.String(255), comment="头像地址")
    intro = db.Column(db.Text, comment="简介")
    create_time = db.Column(db.DateTime, default=datetime.utcnow, comment="账号创建时间")
    join_time = db.Column(db.DateTime, comment="入职/开卡时间")
    expire_date = db.Column(db.Date, comment="到期/停卡时间")

    user = db.relationship("User", back_populates="profile")
```

### 10.3 Slogan 和活动模型

文件：`app/models/activity.py`

```python
from datetime import datetime

from app.extensions import db


class SloganInfo(db.Model):
    __tablename__ = "t_slogan_info"

    slogan_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="标语ID")
    slogan_name = db.Column(db.String(64), nullable=False, comment="标语名称")
    slogan_content = db.Column(db.String(255), nullable=False, comment="标语内容")
    slogan_image_url = db.Column(db.String(255), comment="图片链接")
    status = db.Column(db.Integer, default=1, comment="状态：0-禁用，1-启用")

    def __repr__(self):
        return f"<SloganInfo {self.slogan_name}>"


class ActivityEvent(db.Model):
    __tablename__ = "t_activity_event"

    event_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="活动ID")
    title = db.Column(db.String(255), nullable=False, comment="活动标题")
    event_date = db.Column(db.Date, nullable=False, comment="活动日期")
    location = db.Column(db.String(255), nullable=False, comment="活动地点")
    status = db.Column(db.String(32), nullable=False, comment="状态")
    description = db.Column(db.Text, nullable=False, comment="活动描述")
    tags = db.Column(db.String(255), comment="标签")
    prize = db.Column(db.String(100), comment="奖金/奖品描述")
    scale = db.Column(db.String(100), comment="活动规模")
    created_at = db.Column(db.DateTime, default=datetime.utcnow, comment="创建时间")
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, comment="更新时间")

    def __repr__(self):
        return f"<ActivityEvent {self.title}>"
```

### 10.4 门店模型

文件：`app/models/store.py`

```python
from datetime import datetime

from app.extensions import db


class StoreProvince(db.Model):
    __tablename__ = "t_store_province"

    province_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="省份ID")
    province_name = db.Column(db.String(64), nullable=False, unique=True, comment="省份名称")
    center_lng = db.Column(db.Numeric(10, 6), comment="省份中心经度")
    center_lat = db.Column(db.Numeric(10, 6), comment="省份中心纬度")

    stores = db.relationship("Store", back_populates="province")

    def __repr__(self):
        return f"<StoreProvince {self.province_name}>"


class Store(db.Model):
    __tablename__ = "t_store"

    store_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="门店ID")
    store_name = db.Column(db.String(64), nullable=False, comment="门店名称")
    store_type = db.Column(db.Integer, nullable=False, comment="门店类型：1-铁馆，2-商业私教馆")
    province_id = db.Column(db.BigInteger, db.ForeignKey("t_store_province.province_id"), nullable=False, comment="省份ID")
    province_name = db.Column(db.String(64), comment="省份名称")
    city = db.Column(db.String(64), comment="城市")
    district = db.Column(db.String(64), comment="区/县")
    address = db.Column(db.String(255), comment="详细地址")
    store_phone = db.Column(db.String(20), comment="门店电话")
    store_image_url = db.Column(db.String(255), comment="门店图片")
    store_introduction = db.Column(db.Text, comment="门店介绍")
    business_hours = db.Column(db.String(64), comment="营业时间")
    is_operating = db.Column(db.Integer, default=1, comment="是否营业：1-营业，0-停业")
    store_lng = db.Column(db.Numeric(10, 6), comment="门店经度")
    store_lat = db.Column(db.Numeric(10, 6), comment="门店纬度")

    province = db.relationship("StoreProvince", back_populates="stores")
    user_stores = db.relationship("UserStore", back_populates="store", cascade="all, delete-orphan")

    def __repr__(self):
        return f"<Store {self.store_name}>"


class UserStore(db.Model):
    __tablename__ = "y_user_store"
    __table_args__ = (
        db.Index("idx_user_time", "user_id", "created_at"),
        db.Index("idx_user_store", "user_id", "store_id"),
        db.Index("idx_user_role_store", "user_id", "role_id", "store_id"),
    )

    id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="记录ID")
    user_id = db.Column(db.BigInteger, db.ForeignKey("sys_user.user_id"), nullable=False, comment="用户ID")
    role_id = db.Column(db.BigInteger, db.ForeignKey("sys_role.id"), nullable=False, comment="角色ID")
    store_id = db.Column(db.BigInteger, db.ForeignKey("t_store.store_id"), nullable=False, comment="门店ID")
    created_at = db.Column(db.DateTime, default=datetime.utcnow, comment="关联时间")

    user = db.relationship("User")
    role = db.relationship("Role")
    store = db.relationship("Store", back_populates="user_stores")
```

### 10.5 课程模型

文件：`app/models/course.py`

```python
from datetime import datetime

from app.extensions import db


class CourseCategory(db.Model):
    __tablename__ = "t_course_category"

    category_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="分类ID")
    category_name = db.Column(db.String(64), nullable=False, comment="分类名称")
    category_url = db.Column(db.String(255), comment="分类图片URL")
    description = db.Column(db.String(500), comment="分类描述")
    status = db.Column(db.Integer, default=1, comment="状态")

    courses = db.relationship("Course", back_populates="category")

    def __repr__(self):
        return f"<CourseCategory {self.category_name}>"


class Course(db.Model):
    __tablename__ = "t_course"

    course_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="课程ID")
    course_name = db.Column(db.String(64), nullable=False, comment="课程名称")
    category_id = db.Column(db.BigInteger, db.ForeignKey("t_course_category.category_id"), nullable=False, comment="课程分类ID")
    course_difficulty = db.Column(db.Integer, comment="课程难度")
    duration_minutes = db.Column(db.Integer, comment="课程时长(分钟)")
    max_participants = db.Column(db.Integer, comment="最大参与人数")
    schedule_info = db.Column(db.Text, comment="排期信息")
    description = db.Column(db.Text, comment="课程详细描述")
    status = db.Column(db.Integer, default=1, comment="状态")

    category = db.relationship("CourseCategory", back_populates="courses")
    favorites = db.relationship("UserCourseFavorite", back_populates="course", cascade="all, delete-orphan")
    schedules = db.relationship("StoreCourseSchedule", back_populates="course", cascade="all, delete-orphan")

    def __repr__(self):
        return f"<Course {self.course_name}>"


class UserCourseFavorite(db.Model):
    __tablename__ = "y_user_course_favorite"
    __table_args__ = (
        db.UniqueConstraint("user_id", "course_id", name="uk_user_course"),
    )

    favorite_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="收藏记录ID")
    user_id = db.Column(db.BigInteger, db.ForeignKey("sys_user.user_id"), nullable=False, comment="用户ID")
    course_id = db.Column(db.BigInteger, db.ForeignKey("t_course.course_id"), nullable=False, comment="课程ID")
    created_at = db.Column(db.DateTime, default=datetime.utcnow, comment="收藏时间")

    user = db.relationship("User")
    course = db.relationship("Course", back_populates="favorites")


class StoreCourseSchedule(db.Model):
    __tablename__ = "t_store_course_schedule"
    __table_args__ = (
        db.Index("idx_store_date", "store_id", "course_date"),
        db.Index("idx_course_time", "course_id", "start_time"),
    )

    schedule_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="排期ID")
    store_id = db.Column(db.BigInteger, db.ForeignKey("t_store.store_id"), nullable=False, comment="所属门店ID")
    course_id = db.Column(db.BigInteger, db.ForeignKey("t_course.course_id"), nullable=False, comment="课程ID")
    coach_id = db.Column(db.BigInteger, db.ForeignKey("sys_user.user_id"), comment="教练用户ID")
    course_date = db.Column(db.Date, nullable=False, comment="上课日期")
    start_time = db.Column(db.DateTime, nullable=False, comment="开始时间")
    classroom_name = db.Column(db.String(64), comment="教室名称")
    status = db.Column(db.Integer, default=1, comment="状态：1-正常，0-取消")
    created_at = db.Column(db.DateTime, default=datetime.utcnow, comment="创建时间")
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, comment="更新时间")

    store = db.relationship("Store")
    course = db.relationship("Course", back_populates="schedules")
    coach = db.relationship("User")
```

### 10.6 动作库模型

文件：`app/models/action.py`

```python
from datetime import datetime

from app.extensions import db


class ActionCategory(db.Model):
    __tablename__ = "t_action_category"

    category_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="分类ID")
    category_name = db.Column(db.String(64), nullable=False, comment="分类名称")
    category_image_url = db.Column(db.String(255), comment="分类图标/图片")

    actions = db.relationship("Action", back_populates="category")

    def __repr__(self):
        return f"<ActionCategory {self.category_name}>"


class Action(db.Model):
    __tablename__ = "t_action"

    action_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="动作ID")
    action_name = db.Column(db.String(64), nullable=False, comment="动作名称")
    category_id = db.Column(db.BigInteger, db.ForeignKey("t_action_category.category_id"), nullable=False, comment="动作分类ID")
    action_difficulty = db.Column(db.Integer, comment="动作难度")
    action_image_url = db.Column(db.String(255), comment="动作演示图/视频封面")
    action_steps = db.Column(db.Text, comment="动作步骤文案")
    attention_points = db.Column(db.Text, comment="注意事项")
    applicable_equipment = db.Column(db.String(255), comment="适用器械")
    applicable_store_type = db.Column(db.Integer, comment="适用门店类型")

    category = db.relationship("ActionCategory", back_populates="actions")
    favorites = db.relationship("UserActionFavorite", back_populates="action", cascade="all, delete-orphan")

    def __repr__(self):
        return f"<Action {self.action_name}>"


class UserActionFavorite(db.Model):
    __tablename__ = "y_user_action_favorite"
    __table_args__ = (
        db.UniqueConstraint("user_id", "action_id", name="uk_user_action"),
    )

    favorite_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="收藏记录ID")
    user_id = db.Column(db.BigInteger, db.ForeignKey("sys_user.user_id"), nullable=False, comment="用户ID")
    action_id = db.Column(db.BigInteger, db.ForeignKey("t_action.action_id"), nullable=False, comment="动作ID")
    created_at = db.Column(db.DateTime, default=datetime.utcnow, comment="收藏时间")

    user = db.relationship("User")
    action = db.relationship("Action", back_populates="favorites")
```

### 10.7 论坛模型

文件：`app/models/forum.py`

```python
from datetime import datetime

from app.extensions import db


class ForumPost(db.Model):
    __tablename__ = "t_forum_post"

    post_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="帖子ID")
    user_id = db.Column(db.BigInteger, db.ForeignKey("sys_user.user_id"), nullable=False, comment="发帖用户ID")
    title = db.Column(db.String(255), nullable=False, comment="帖子标题")
    content = db.Column(db.Text, nullable=False, comment="帖子内容")
    like_count = db.Column(db.Integer, default=0, comment="点赞数")
    comment_count = db.Column(db.Integer, default=0, comment="评论数")
    created_at = db.Column(db.DateTime, default=datetime.utcnow, comment="创建时间")
    status = db.Column(db.Integer, default=1, comment="状态")

    user = db.relationship("User")
    comments = db.relationship("ForumComment", back_populates="post", cascade="all, delete-orphan")
    likes = db.relationship("ForumLike", back_populates="post", cascade="all, delete-orphan")

    def __repr__(self):
        return f"<ForumPost {self.title}>"


class ForumComment(db.Model):
    __tablename__ = "t_forum_comment"

    comment_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="评论ID")
    post_id = db.Column(db.BigInteger, db.ForeignKey("t_forum_post.post_id"), nullable=False, comment="所属帖子ID")
    user_id = db.Column(db.BigInteger, db.ForeignKey("sys_user.user_id"), nullable=False, comment="评论用户ID")
    parent_id = db.Column(db.BigInteger, db.ForeignKey("t_forum_comment.comment_id"), comment="父评论ID")
    content = db.Column(db.Text, nullable=False, comment="评论内容")
    created_at = db.Column(db.DateTime, default=datetime.utcnow, comment="创建时间")
    status = db.Column(db.Integer, default=1, comment="状态")

    post = db.relationship("ForumPost", back_populates="comments")
    user = db.relationship("User")
    parent = db.relationship("ForumComment", remote_side=[comment_id], backref="children")


class ForumLike(db.Model):
    __tablename__ = "t_forum_like"
    __table_args__ = (
        db.UniqueConstraint("post_id", "user_id", name="uk_post_user"),
    )

    like_id = db.Column(db.BigInteger, primary_key=True, autoincrement=True, comment="点赞ID")
    post_id = db.Column(db.BigInteger, db.ForeignKey("t_forum_post.post_id"), nullable=False, comment="帖子ID")
    user_id = db.Column(db.BigInteger, db.ForeignKey("sys_user.user_id"), nullable=False, comment="点赞用户ID")
    created_at = db.Column(db.DateTime, default=datetime.utcnow, comment="点赞时间")

    post = db.relationship("ForumPost", back_populates="likes")
    user = db.relationship("User")
```

---

## 11. 基础模板

### 11.1 前台基础布局

文件：`app/templates/web/layout.html`

```html
<!doctype html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}JoyFit{% endblock %}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/web.css') }}">
</head>
<body>
    <header class="site-header">
        <a class="brand" href="{{ url_for('web.index') }}">JoyFit</a>
        <nav class="site-nav">
            <a href="{{ url_for('web.index') }}">首页</a>
            <a href="#">课程</a>
            <a href="#">动作库</a>
            <a href="#">活动</a>
            <a href="#">社区</a>
            <a href="{{ url_for('auth.login') }}">登录</a>
        </nav>
    </header>

    <main class="site-main">
        {% block content %}{% endblock %}
    </main>
</body>
</html>
```

### 11.2 前台首页

文件：`app/templates/web/index.html`

```html
{% extends "web/layout.html" %}

{% block title %}JoyFit 首页{% endblock %}

{% block content %}
<section class="hero">
    <div>
        <p class="eyebrow">JoyFit Training</p>
        <h1>训练、课程、门店和社区的一体化健身平台</h1>
        <p class="hero-text">这里是前台首页基础模板。后续阶段会接入 Banner、Slogan、活动、课程和论坛数据。</p>
    </div>
</section>
{% endblock %}
```

### 11.3 后台基础布局

文件：`app/templates/admin/layout.html`

```html
<!doctype html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}JoyFit 后台{% endblock %}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/admin.css') }}">
</head>
<body>
    <aside class="admin-sidebar">
        <div class="admin-brand">JoyFit Admin</div>
        <nav class="admin-menu">
            <a href="{{ url_for('admin.dashboard') }}">控制台</a>
            <a href="#">用户管理</a>
            <a href="#">角色权限</a>
            <a href="#">门店管理</a>
            <a href="#">课程管理</a>
            <a href="#">动作库</a>
            <a href="#">活动配置</a>
            <a href="#">论坛管理</a>
        </nav>
    </aside>

    <section class="admin-shell">
        <header class="admin-topbar">
            <span>{% block page_name %}控制台{% endblock %}</span>
            <a href="{{ url_for('auth.login') }}">登录页</a>
        </header>
        <main class="admin-main">
            {% block content %}{% endblock %}
        </main>
    </section>
</body>
</html>
```

### 11.4 后台首页

文件：`app/templates/admin/dashboard.html`

```html
{% extends "admin/layout.html" %}

{% block title %}后台控制台{% endblock %}
{% block page_name %}后台控制台{% endblock %}

{% block content %}
<div class="page-title">
    <h1>控制台</h1>
    <p>项目基础骨架已完成。后续阶段会逐步接入登录、权限、用户、门店、课程、动作、活动和论坛模块。</p>
</div>

<section class="stat-grid">
    <article class="stat-card">
        <span>用户模块</span>
        <strong>待开发</strong>
    </article>
    <article class="stat-card">
        <span>门店模块</span>
        <strong>待开发</strong>
    </article>
    <article class="stat-card">
        <span>课程模块</span>
        <strong>待开发</strong>
    </article>
    <article class="stat-card">
        <span>社区模块</span>
        <strong>待开发</strong>
    </article>
</section>
{% endblock %}
```

### 11.5 登录页占位模板

文件：`app/templates/auth/login.html`

```html
<!doctype html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>登录 JoyFit 后台</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/admin.css') }}">
</head>
<body class="login-page">
    <form class="login-panel" method="post">
        <h1>JoyFit 后台登录</h1>
        <label>
            用户名
            <input type="text" name="username" placeholder="请输入用户名">
        </label>
        <label>
            密码
            <input type="password" name="password" placeholder="请输入密码">
        </label>
        <button type="submit">登录</button>
        <p>当前为第 1 阶段占位页面，登录逻辑将在第 2 阶段实现。</p>
    </form>
</body>
</html>
```

### 11.6 错误页

文件：`app/templates/errors/404.html`

```html
<!doctype html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <title>页面不存在</title>
</head>
<body>
    <h1>404</h1>
    <p>页面不存在。</p>
    <a href="{{ url_for('web.index') }}">返回首页</a>
</body>
</html>
```

文件：`app/templates/errors/500.html`

```html
<!doctype html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <title>服务器错误</title>
</head>
<body>
    <h1>500</h1>
    <p>服务器暂时无法处理请求。</p>
    <a href="{{ url_for('web.index') }}">返回首页</a>
</body>
</html>
```

---

## 12. 基础样式

### 12.1 前台样式

文件：`app/static/css/web.css`

```css
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    color: #1f2937;
    background: #f8fafc;
    font-family: Arial, "Microsoft YaHei", sans-serif;
}

a {
    color: inherit;
    text-decoration: none;
}

.site-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 64px;
    padding: 0 40px;
    background: #ffffff;
    border-bottom: 1px solid #e5e7eb;
}

.brand {
    color: #111827;
    font-size: 22px;
    font-weight: 700;
}

.site-nav {
    display: flex;
    gap: 24px;
    color: #4b5563;
    font-size: 14px;
}

.site-nav a:hover {
    color: #0f766e;
}

.site-main {
    min-height: calc(100vh - 64px);
}

.hero {
    display: flex;
    align-items: center;
    min-height: 520px;
    padding: 80px 40px;
    color: #ffffff;
    background:
        linear-gradient(90deg, rgba(15, 23, 42, 0.86), rgba(15, 118, 110, 0.62)),
        url("https://images.unsplash.com/photo-1534438327276-14e5300c3a48?auto=format&fit=crop&w=1600&q=80");
    background-position: center;
    background-size: cover;
}

.hero > div {
    max-width: 760px;
}

.eyebrow {
    margin: 0 0 12px;
    color: #99f6e4;
    font-size: 14px;
    font-weight: 700;
}

.hero h1 {
    margin: 0;
    max-width: 720px;
    font-size: 48px;
    line-height: 1.18;
}

.hero-text {
    max-width: 620px;
    margin: 20px 0 0;
    color: #d1fae5;
    font-size: 18px;
    line-height: 1.8;
}
```

### 12.2 后台样式

文件：`app/static/css/admin.css`

```css
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    color: #111827;
    background: #f3f4f6;
    font-family: Arial, "Microsoft YaHei", sans-serif;
}

a {
    color: inherit;
    text-decoration: none;
}

.admin-sidebar {
    position: fixed;
    inset: 0 auto 0 0;
    width: 232px;
    background: #111827;
    color: #d1d5db;
}

.admin-brand {
    height: 64px;
    padding: 20px 24px;
    color: #ffffff;
    font-size: 18px;
    font-weight: 700;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.admin-menu {
    display: grid;
    gap: 4px;
    padding: 16px 12px;
}

.admin-menu a {
    display: block;
    padding: 12px;
    border-radius: 6px;
    font-size: 14px;
}

.admin-menu a:hover {
    color: #ffffff;
    background: #1f2937;
}

.admin-shell {
    margin-left: 232px;
}

.admin-topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 64px;
    padding: 0 28px;
    background: #ffffff;
    border-bottom: 1px solid #e5e7eb;
}

.admin-main {
    padding: 28px;
}

.page-title h1 {
    margin: 0 0 8px;
    font-size: 28px;
}

.page-title p {
    margin: 0;
    color: #6b7280;
}

.stat-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 16px;
    margin-top: 24px;
}

.stat-card {
    padding: 20px;
    background: #ffffff;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
}

.stat-card span {
    display: block;
    color: #6b7280;
    font-size: 14px;
}

.stat-card strong {
    display: block;
    margin-top: 10px;
    font-size: 24px;
}

.login-page {
    display: grid;
    place-items: center;
    min-height: 100vh;
    background: #eef2ff;
}

.login-panel {
    display: grid;
    gap: 16px;
    width: min(420px, calc(100vw - 32px));
    padding: 28px;
    background: #ffffff;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
}

.login-panel h1 {
    margin: 0;
    font-size: 24px;
}

.login-panel label {
    display: grid;
    gap: 8px;
    color: #374151;
    font-size: 14px;
}

.login-panel input {
    height: 42px;
    padding: 0 12px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
}

.login-panel button {
    height: 42px;
    color: #ffffff;
    background: #0f766e;
    border: 0;
    border-radius: 6px;
    cursor: pointer;
}

.login-panel p {
    margin: 0;
    color: #6b7280;
    font-size: 13px;
    line-height: 1.6;
}
```

---

## 13. 数据库准备

### 13.1 创建数据库

在 MySQL 中执行：

```sql
CREATE DATABASE IF NOT EXISTS `flask_joyfit_fullstack`
CHARACTER SET utf8mb4
COLLATE utf8mb4_general_ci;
```

### 13.2 初始化迁移

第一次使用 Flask-Migrate 时执行：

```powershell
flask db init
```

生成迁移脚本：

```powershell
flask db migrate -m "init database models"
```

执行迁移：

```powershell
flask db upgrade
```

如果模型没有被识别，确认 `app/models/__init__.py` 已经导入全部模型，并在 `app/__init__.py` 中加入下面这一行：

```python
from . import models
```

推荐放在 `register_extensions(app)` 之后、`register_blueprints(app)` 之前：

```python
def create_app(config_name=None):
    app = Flask(__name__)

    env_name = config_name or os.getenv("FLASK_ENV", "development")
    app.config.from_object(config_map.get(env_name, config_map["development"]))

    register_extensions(app)

    from . import models

    register_blueprints(app)
    register_error_handlers(app)

    return app
```

---

## 14. 推荐补充：让模型自动被迁移工具发现

为了避免忘记导入模型，最终版 `app/__init__.py` 建议使用下面这份：

```python
import os

from flask import Flask, render_template

from config import config_map
from .extensions import db, migrate


def create_app(config_name=None):
    app = Flask(__name__)

    env_name = config_name or os.getenv("FLASK_ENV", "development")
    app.config.from_object(config_map.get(env_name, config_map["development"]))

    register_extensions(app)

    from . import models

    register_blueprints(app)
    register_error_handlers(app)

    return app


def register_extensions(app):
    db.init_app(app)
    migrate.init_app(app, db)


def register_blueprints(app):
    from .blueprints.admin import admin_bp
    from .blueprints.auth import auth_bp
    from .blueprints.web import web_bp

    app.register_blueprint(web_bp)
    app.register_blueprint(auth_bp, url_prefix="/auth")
    app.register_blueprint(admin_bp, url_prefix="/admin")


def register_error_handlers(app):
    @app.errorhandler(404)
    def page_not_found(error):
        return render_template("errors/404.html"), 404

    @app.errorhandler(500)
    def internal_server_error(error):
        return render_template("errors/500.html"), 500
```

---

## 15. 启动项目

```powershell
flask run
```

或者：

```powershell
python run.py
```

访问地址：

```text
前台首页：http://127.0.0.1:5000/
后台首页：http://127.0.0.1:5000/admin/
登录页面：http://127.0.0.1:5000/auth/login
```

---

## 16. 第 1 阶段验收标准

| 检查项 | 预期结果 |
| --- | --- |
| `python run.py` | Flask 服务正常启动 |
| `/` | 可以访问前台首页 |
| `/admin/` | 可以访问后台控制台 |
| `/auth/login` | 可以访问登录占位页 |
| `flask db migrate` | 可以生成迁移文件 |
| `flask db upgrade` | 可以在 MySQL 中创建数据表 |
| 数据库表数量 | 生成 20 张业务表和 Alembic 版本表 |

---

## 17. 下一阶段衔接

第 2 阶段进入登录认证模块，建议基于当前项目继续补充：

| 文件 | 第 2 阶段要做的事 |
| --- | --- |
| `app/blueprints/auth/routes.py` | 增加登录、退出、Session 写入逻辑 |
| `app/models/system.py` | 使用 `User.check_password()` 校验密码 |
| `app/blueprints/admin/routes.py` | 增加登录拦截 |
| `app/templates/auth/login.html` | 展示错误消息、补充 CSRF 或基础表单校验 |
| `app/templates/admin/layout.html` | 展示当前登录用户和退出按钮 |

---

## 18. 问题记录：TemplateNotFound web/index.html

### 18.1 报错现象

启动项目并访问前台首页 `/` 时，页面报错：

```text
jinja2.exceptions.TemplateNotFound: web/index.html
```

对应路由代码通常是：

```python
@web_bp.route("/")
def index():
    return render_template("web/index.html")
```

### 18.2 问题原因

这个报错说明 Flask 已经成功找到并执行了路由函数，但 Jinja2 没有在模板目录中找到 `web/index.html`。

`render_template("web/index.html")` 对应的真实文件路径必须是：

```text
flask_joyfit_fullstack/app/templates/web/index.html
```

常见原因：

| 原因 | 说明 |
| --- | --- |
| 没创建模板文件 | 缺少 `app/templates/web/index.html` |
| 模板目录写错 | 必须是 `templates`，不是 `template` |
| 文件位置放错 | 必须在 `app/templates/web/` 下 |
| 项目运行目录不一致 | 实际运行的是另一个 `flask_joyfit_fullstack` 项目 |
| 文件名不一致 | 必须是 `index.html`，不能写成 `index.htm` 或其他名称 |

### 18.3 修改方法

先进入实际运行的 Flask 项目根目录：

```bash
cd /Users/malyu/Downloads/PythonCode/flask_joyfit_fullstack
```

创建模板目录：

```bash
mkdir -p app/templates/web
```

确认或创建文件：

```text
app/templates/web/index.html
```

内容如下：

```html
{% extends "web/layout.html" %}

{% block title %}JoyFit 首页{% endblock %}

{% block content %}
<section class="hero">
    <div>
        <p class="eyebrow">JoyFit Training</p>
        <h1>训练、课程、门店和社区的一体化健身平台</h1>
        <p class="hero-text">这里是前台首页基础模板。后续阶段会接入 Banner、Slogan、活动、课程和论坛数据。</p>
    </div>
</section>
{% endblock %}
```

同时确认前台基础布局文件也存在：

```text
app/templates/web/layout.html
```

如果不存在，按照本文第 11.1 节创建 `app/templates/web/layout.html`。

### 18.4 验证方法

重新启动 Flask：

```bash
python run.py
```

访问：

```text
http://127.0.0.1:5000/
```

如果页面正常显示 JoyFit 首页，说明问题已解决。
