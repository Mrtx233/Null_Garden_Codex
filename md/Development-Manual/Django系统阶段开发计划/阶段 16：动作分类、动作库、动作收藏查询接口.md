本阶段目标：

```plain
1. 查询动作分类列表
2. 查询动作列表
3. 查询动作详情
4. 查询当前用户收藏的动作
5. 收藏动作
6. 取消收藏动作
```

本阶段基于你前面设计的三张表：

```plain
t_action_category          动作分类表
t_action                   动作库条目表
y_user_action_favorite     用户动作收藏表
```

你的 SQL 文件中这三张表分别用于动作分类、动作主数据、用户动作收藏关系，其中 `t_action` 通过 `category_id` 关联动作分类，`y_user_action_favorite` 通过 `user_id` 和 `action_id` 关联用户和动作。

---

# 一、本阶段新增接口
我们把动作相关接口统一放在：

```plain
/api/exercise/
```

最终接口如下：

| 请求方式 | 接口 | 作用 | 是否需要登录 |
| --- | --- | --- | --- |
| GET | `/api/exercise/categories/` | 动作分类列表 | 需要 |
| GET | `/api/exercise/actions/` | 动作列表 | 需要 |
| GET | `/api/exercise/actions/<action_id>/` | 动作详情 | 需要 |
| GET | `/api/exercise/my-favorites/` | 我的动作收藏 | 需要 |
| POST | `/api/exercise/favorites/` | 收藏动作 | 需要 |
| DELETE | `/api/exercise/favorites/<favorite_id>/` | 取消收藏动作 | 需要 |


---

# 二、接口效果说明
## 1. 动作分类列表
```plain
GET /api/exercise/categories/
```

返回：

```json
{
  "code": 200,
  "message": "获取动作分类成功",
  "data": [
    {
      "category_id": 1,
      "category_name": "胸部",
      "category_image_url": "https://example.com/action-chest.jpg"
    }
  ]
}
```

---

## 2. 动作列表
```plain
GET /api/exercise/actions/
```

支持筛选：

```plain
category_id              按动作分类筛选
action_difficulty        按动作难度筛选
applicable_store_type    按适用门店类型筛选
keyword                  按动作名称模糊搜索
```

例如：

```plain
GET /api/exercise/actions/?category_id=1
GET /api/exercise/actions/?keyword=深蹲
GET /api/exercise/actions/?applicable_store_type=1
```

---

## 3. 动作详情
```plain
GET /api/exercise/actions/1/
```

返回某一个动作的详细信息。

---

## 4. 我的动作收藏
```plain
GET /api/exercise/my-favorites/
```

后端会根据 JWT 找到当前用户，然后返回当前用户收藏的动作。

---

## 5. 收藏动作
```plain
POST /api/exercise/favorites/
```

请求体：

```json
{
  "action_id": 1
}
```

---

## 6. 取消收藏动作
```plain
DELETE /api/exercise/favorites/1/
```

这里的 `1` 是收藏记录的 `favorite_id`，不是动作 ID。

---

# 三、修改 `exercise/views.py`
打开文件：

```plain
backend/exercise/views.py
```

把内容替换成下面完整代码。

## `backend/exercise/views.py`
```python
import json

from django.views import View
from django.views.decorators.csrf import csrf_exempt
from django.utils.decorators import method_decorator

from rbac.auth import login_required
from rbac.responses import json_response

from .models import (
    ActionCategory,
    Action,
    UserActionFavorite,
)


def get_json_body(request):
    """
    获取 JSON 请求体
    """

    try:
        body = request.body.decode("utf-8")

        if not body:
            return {}

        return json.loads(body)

    except json.JSONDecodeError:
        return None


def format_datetime_value(value):
    """
    datetime 转字符串
    """

    if value is None:
        return None

    return value.strftime("%Y-%m-%d %H:%M:%S")


def serialize_category(category):
    """
    动作分类对象转 JSON 字典
    """

    return {
        "category_id": category.category_id,
        "category_name": category.category_name,
        "category_image_url": category.category_image_url,
    }


def serialize_action(action, current_user=None):
    """
    动作对象转 JSON 字典
    """

    is_favorite = False
    favorite_id = None

    if current_user:
        favorite = UserActionFavorite.objects.filter(
            user=current_user,
            action=action
        ).first()

        if favorite:
            is_favorite = True
            favorite_id = favorite.favorite_id

    return {
        "action_id": action.action_id,
        "action_name": action.action_name,

        "category_id": action.category_id,
        "category_name": action.category.category_name,

        "action_difficulty": action.action_difficulty,
        "action_image_url": action.action_image_url,

        "action_steps": action.action_steps,
        "attention_points": action.attention_points,
        "applicable_equipment": action.applicable_equipment,

        "applicable_store_type": action.applicable_store_type,
        "applicable_store_type_text": (
            action.get_applicable_store_type_display()
            if action.applicable_store_type is not None
            else None
        ),

        "is_favorite": is_favorite,
        "favorite_id": favorite_id,
    }


def serialize_favorite(favorite):
    """
    用户动作收藏对象转 JSON 字典
    """

    action = favorite.action

    return {
        "favorite_id": favorite.favorite_id,
        "created_at": format_datetime_value(favorite.created_at),
        "action": serialize_action(action, favorite.user),
    }


class ActionCategoryListView(View):
    """
    动作分类列表接口

    GET /api/exercise/categories/
    """

    @login_required
    def get(self, request):
        categories = ActionCategory.objects.all().order_by("category_id")

        data = []

        for category in categories:
            data.append(serialize_category(category))

        return json_response(
            data=data,
            message="获取动作分类成功",
        )


class ActionListView(View):
    """
    动作列表接口

    GET /api/exercise/actions/

    可选查询参数：
    category_id
    action_difficulty
    applicable_store_type
    keyword
    """

    @login_required
    def get(self, request):
        current_user = request.current_user

        queryset = Action.objects.select_related("category").all().order_by("action_id")

        category_id = request.GET.get("category_id")
        action_difficulty = request.GET.get("action_difficulty")
        applicable_store_type = request.GET.get("applicable_store_type")
        keyword = request.GET.get("keyword")

        if category_id:
            queryset = queryset.filter(category_id=category_id)

        if action_difficulty:
            queryset = queryset.filter(action_difficulty=action_difficulty)

        if applicable_store_type:
            queryset = queryset.filter(applicable_store_type=applicable_store_type)

        if keyword:
            queryset = queryset.filter(action_name__icontains=keyword)

        data = []

        for action in queryset:
            data.append(serialize_action(action, current_user))

        return json_response(
            data=data,
            message="获取动作列表成功",
        )


class ActionDetailView(View):
    """
    动作详情接口

    GET /api/exercise/actions/<action_id>/
    """

    @login_required
    def get(self, request, action_id):
        current_user = request.current_user

        action = Action.objects.select_related("category").filter(
            action_id=action_id
        ).first()

        if action is None:
            return json_response(
                message="动作不存在",
                code=404,
                status=404,
            )

        return json_response(
            data=serialize_action(action, current_user),
            message="获取动作详情成功",
        )


class MyActionFavoriteListView(View):
    """
    当前用户动作收藏列表接口

    GET /api/exercise/my-favorites/
    """

    @login_required
    def get(self, request):
        current_user = request.current_user

        favorites = UserActionFavorite.objects.select_related(
            "user",
            "action",
            "action__category",
        ).filter(
            user=current_user
        ).order_by("-created_at")

        data = []

        for favorite in favorites:
            data.append(serialize_favorite(favorite))

        return json_response(
            data=data,
            message="获取我的动作收藏成功",
        )


@method_decorator(csrf_exempt, name="dispatch")
class ActionFavoriteCreateView(View):
    """
    收藏动作接口

    POST /api/exercise/favorites/

    请求体：
    {
        "action_id": 1
    }
    """

    @login_required
    def post(self, request):
        current_user = request.current_user

        data = get_json_body(request)

        if data is None:
            return json_response(
                message="请求体不是合法 JSON",
                code=400,
                status=400,
            )

        action_id = data.get("action_id")

        if not action_id:
            return json_response(
                message="action_id 不能为空",
                code=400,
                status=400,
            )

        action = Action.objects.select_related("category").filter(
            action_id=action_id
        ).first()

        if action is None:
            return json_response(
                message="动作不存在",
                code=404,
                status=404,
            )

        favorite, created = UserActionFavorite.objects.get_or_create(
            user=current_user,
            action=action,
        )

        if not created:
            return json_response(
                data=serialize_favorite(favorite),
                message="你已经收藏过该动作",
            )

        return json_response(
            data=serialize_favorite(favorite),
            message="收藏动作成功",
        )


@method_decorator(csrf_exempt, name="dispatch")
class ActionFavoriteDeleteView(View):
    """
    取消收藏动作接口

    DELETE /api/exercise/favorites/<favorite_id>/
    """

    @login_required
    def delete(self, request, favorite_id):
        current_user = request.current_user

        favorite = UserActionFavorite.objects.select_related(
            "user",
            "action",
            "action__category",
        ).filter(
            favorite_id=favorite_id,
            user=current_user,
        ).first()

        if favorite is None:
            return json_response(
                message="收藏记录不存在",
                code=404,
                status=404,
            )

        favorite.delete()

        return json_response(
            message="取消收藏成功",
        )
```

---

# 四、创建 `exercise/urls.py`
新建文件：

```plain
backend/exercise/urls.py
```

写入：

```python
from django.urls import path

from .views import (
    ActionCategoryListView,
    ActionListView,
    ActionDetailView,
    MyActionFavoriteListView,
    ActionFavoriteCreateView,
    ActionFavoriteDeleteView,
)

urlpatterns = [
    path("categories/", ActionCategoryListView.as_view()),

    path("actions/", ActionListView.as_view()),
    path("actions/<int:action_id>/", ActionDetailView.as_view()),

    path("my-favorites/", MyActionFavoriteListView.as_view()),
    path("favorites/", ActionFavoriteCreateView.as_view()),
    path("favorites/<int:favorite_id>/", ActionFavoriteDeleteView.as_view()),
]
```

---

# 五、修改项目总路由
打开文件：

```plain
backend/config/urls.py
```

你现在应该已经有：

```python
from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path("admin/", admin.site.urls),

    path("api/", include("rbac.urls")),
    path("api/store/", include("store.urls")),
    path("api/course/", include("course.urls")),
]
```

改成：

```python
from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path("admin/", admin.site.urls),

    path("api/", include("rbac.urls")),
    path("api/store/", include("store.urls")),
    path("api/course/", include("course.urls")),
    path("api/exercise/", include("exercise.urls")),
]
```

现在动作接口最终地址就是：

```plain
/api/exercise/categories/
/api/exercise/actions/
/api/exercise/actions/<action_id>/
/api/exercise/my-favorites/
/api/exercise/favorites/
/api/exercise/favorites/<favorite_id>/
```

---

# 六、启动后端
确认你在：

```plain
rbac_project/backend
```

启动后端：

```bash
python manage.py runserver
```

---

# 七、测试接口
## 1. 登录 admin 获取 token
macOS / Linux：

```bash
curl -X POST http://127.0.0.1:8000/api/login/ \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'
```

Windows PowerShell：

```powershell
curl -Method POST "http://127.0.0.1:8000/api/login/" `
  -Headers @{"Content-Type"="application/json"} `
  -Body '{"username":"admin","password":"123456"}'
```

复制返回结果里的：

```plain
access_token
```

下面用它替换：

```plain
你的admin_access_token
```

---

## 2. 查询动作分类
macOS / Linux：

```bash
curl -X GET http://127.0.0.1:8000/api/exercise/categories/ \
  -H "Authorization: Bearer 你的admin_access_token"
```

Windows PowerShell：

```powershell
curl -Method GET "http://127.0.0.1:8000/api/exercise/categories/" `
  -Headers @{"Authorization"="Bearer 你的admin_access_token"}
```

正常应该看到：

```plain
胸部
腿部
核心
```

---

## 3. 查询动作列表
macOS / Linux：

```bash
curl -X GET http://127.0.0.1:8000/api/exercise/actions/ \
  -H "Authorization: Bearer 你的admin_access_token"
```

Windows PowerShell：

```powershell
curl -Method GET "http://127.0.0.1:8000/api/exercise/actions/" `
  -Headers @{"Authorization"="Bearer 你的admin_access_token"}
```

正常应该看到：

```plain
俯卧撑
深蹲
平板支撑
```

---

## 4. 按关键词搜索动作
```bash
curl -X GET "http://127.0.0.1:8000/api/exercise/actions/?keyword=深蹲" \
  -H "Authorization: Bearer 你的admin_access_token"
```

应该只返回：

```plain
深蹲
```

---

## 5. 按分类筛选动作
假设 `胸部` 的 `category_id` 是 1：

```bash
curl -X GET "http://127.0.0.1:8000/api/exercise/actions/?category_id=1" \
  -H "Authorization: Bearer 你的admin_access_token"
```

应该返回胸部分类下的动作：

```plain
俯卧撑
```

---

## 6. 按门店类型筛选动作
门店类型：

```plain
1：铁馆
2：商业私教馆
```

请求：

```bash
curl -X GET "http://127.0.0.1:8000/api/exercise/actions/?applicable_store_type=1" \
  -H "Authorization: Bearer 你的admin_access_token"
```

应该返回适合铁馆的动作。

---

## 7. 查询动作详情
假设动作 ID 是 1：

```bash
curl -X GET http://127.0.0.1:8000/api/exercise/actions/1/ \
  -H "Authorization: Bearer 你的admin_access_token"
```

正常返回：

```json
{
  "code": 200,
  "message": "获取动作详情成功",
  "data": {
    "action_id": 1,
    "action_name": "俯卧撑",
    "category_id": 1,
    "category_name": "胸部",
    "action_difficulty": 1,
    "action_steps": "双手撑地，身体保持直线，屈肘下降后推起。",
    "attention_points": "腰背保持稳定，不要塌腰。",
    "applicable_equipment": "徒手",
    "applicable_store_type": 1,
    "applicable_store_type_text": "铁馆",
    "is_favorite": true,
    "favorite_id": 1
  }
}
```

---

## 8. 查询我的动作收藏
```bash
curl -X GET http://127.0.0.1:8000/api/exercise/my-favorites/ \
  -H "Authorization: Bearer 你的admin_access_token"
```

admin 正常应该看到：

```plain
俯卧撑
```

---

## 9. 收藏动作
假设动作 ID 是 2：

macOS / Linux：

```bash
curl -X POST http://127.0.0.1:8000/api/exercise/favorites/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 你的admin_access_token" \
  -d '{"action_id":2}'
```

Windows PowerShell：

```powershell
curl -Method POST "http://127.0.0.1:8000/api/exercise/favorites/" `
  -Headers @{
    "Content-Type"="application/json"
    "Authorization"="Bearer 你的admin_access_token"
  } `
  -Body '{"action_id":2}'
```

正常返回：

```json
{
  "code": 200,
  "message": "收藏动作成功",
  "data": {
    "favorite_id": 3
  }
}
```

如果已经收藏过，会返回：

```json
{
  "code": 200,
  "message": "你已经收藏过该动作"
}
```

---

## 10. 取消收藏动作
假设收藏记录 ID 是 3：

```bash
curl -X DELETE http://127.0.0.1:8000/api/exercise/favorites/3/ \
  -H "Authorization: Bearer 你的admin_access_token"
```

正常返回：

```json
{
  "code": 200,
  "message": "取消收藏成功",
  "data": null
}
```

注意：

```plain
这里只能取消当前登录用户自己的收藏。
不能删除别人的收藏。
```

---

# 八、测试 zhangsan
先登录 zhangsan：

```bash
curl -X POST http://127.0.0.1:8000/api/login/ \
  -H "Content-Type: application/json" \
  -d '{"username":"zhangsan","password":"123456"}'
```

复制 zhangsan 的 `access_token`。

查询收藏：

```bash
curl -X GET http://127.0.0.1:8000/api/exercise/my-favorites/ \
  -H "Authorization: Bearer 你的zhangsan_access_token"
```

zhangsan 正常应该看到：

```plain
深蹲
```

收藏动作：

```bash
curl -X POST http://127.0.0.1:8000/api/exercise/favorites/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 你的zhangsan_access_token" \
  -d '{"action_id":1}'
```

这样 zhangsan 就会收藏：

```plain
俯卧撑
```

---

# 九、不带 token 的情况
直接请求：

```bash
curl -X GET http://127.0.0.1:8000/api/exercise/actions/
```

应该返回：

```json
{
  "code": 401,
  "message": "未提供 access_token",
  "data": null
}
```

说明动作接口已经受到登录保护。

---

# 十、常见问题
## 1. 报错：`No module named exercise.urls`
说明你还没有创建：

```plain
backend/exercise/urls.py
```

或者 `config/urls.py` 写错了：

```python
path("api/exercise/", include("exercise.urls")),
```

---

## 2. 报错：`Table 'rbac_db.t_action' doesn't exist`
说明阶段 12 的迁移没有执行成功。

执行：

```bash
python manage.py makemigrations exercise
python manage.py migrate
```

然后再执行：

```bash
python manage.py init_business_data
```

---

## 3. 动作列表是空的
说明阶段 13 的业务测试数据没有初始化。

执行：

```bash
python manage.py init_business_data
```

---

## 4. 收藏动作报重复错误
正常情况下不会报数据库错误，因为接口用了：

```python
UserActionFavorite.objects.get_or_create()
```

如果已经收藏过，会返回：

```plain
你已经收藏过该动作
```

---

## 5. 取消收藏返回 404
原因通常是：

```plain
1. favorite_id 写错了
2. 这个收藏不是当前登录用户的收藏
3. 收藏记录已经被删除
```

你可以先查：

```plain
GET /api/exercise/my-favorites/
```

拿到正确的 `favorite_id` 后再删除。

---

# 十一、阶段 16 完成标准
你需要确认：

```plain
1. backend/exercise/views.py 已经写好
2. backend/exercise/urls.py 已经创建
3. backend/config/urls.py 已经引入 exercise.urls
4. GET /api/exercise/categories/ 可以返回动作分类
5. GET /api/exercise/actions/ 可以返回动作列表
6. GET /api/exercise/actions/<id>/ 可以返回动作详情
7. GET /api/exercise/my-favorites/ 可以返回当前用户收藏动作
8. POST /api/exercise/favorites/ 可以收藏动作
9. DELETE /api/exercise/favorites/<favorite_id>/ 可以取消收藏动作
10. 不带 token 请求动作接口会返回 401
```

---

# 十二、当前模块进度
现在你的项目进度是：

```plain
rbac/
已完成：
- 用户
- 角色
- 权限
- JWT 登录
- RBAC 权限拦截
- 用户档案接口

store/
已完成：
- 省份表
- 门店表
- 用户门店关系表
- 省份查询接口
- 门店查询接口
- 当前用户门店查询接口

course/
已完成：
- 课程分类表
- 课程表
- 用户课程收藏表
- 门店课程排期表
- 课程分类查询接口
- 课程查询接口
- 课程详情接口
- 我的课程收藏接口
- 收藏 / 取消收藏课程接口
- 门店课程排期查询接口

exercise/
已完成：
- 动作分类表
- 动作表
- 用户动作收藏表
- 动作分类查询接口
- 动作查询接口
- 动作详情接口
- 我的动作收藏接口
- 收藏 / 取消收藏动作接口
```

下一阶段建议做：

```plain
阶段 17：Vue3 对接门店、课程、动作接口
```

也就是开始做前端页面：

```plain
门店列表页面
课程列表页面
动作列表页面
我的收藏页面
```

