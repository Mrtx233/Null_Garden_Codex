# 阶段 11：用户档案查询和修改接口
本阶段目标：

```latex
1. 查询当前登录用户的档案信息
2. 修改当前登录用户的档案信息
3. 如果当前用户还没有档案，系统自动创建一条空档案
4. 接口必须登录后才能访问
```

本阶段暂时不做：

```latex
Vue3 档案页面
头像上传
管理员查看别人档案
档案权限 RBAC 控制
```

---

# 一、本阶段接口设计
我们新增一个接口：

```latex
/api/profile/me/
```

它支持两个请求方式：

| 请求方式 | 地址 | 作用 | 是否需要登录 |
| --- | --- | --- | --- |
| GET | `/api/profile/me/` | 查询当前用户档案 | 需要 |
| PUT | `/api/profile/me/` | 修改当前用户档案 | 需要 |


---

# 二、最终效果
用户登录后，带着 `access_token` 请求：

```latex
GET /api/profile/me/
```

后端会根据 token 知道当前用户是谁，然后返回这个用户的档案。

例如 admin 登录后，请求返回：

```json
{
  "code": 200,
  "message": "获取档案成功",
  "data": {
    "profile_id": 1,
    "user_id": 1,
    "username": "admin",
    "real_name": "管理员",
    "level": 10,
    "gender": 1,
    "gender_text": "男",
    "birthday": "1998-01-01",
    "height_cm": "175.00",
    "weight_kg": "70.00",
    "avatar_url": "",
    "intro": "管理员账号",
    "create_time": "2026-05-26 10:00:00",
    "join_time": "2026-05-26 10:00:00",
    "expire_date": "2030-12-31"
  }
}
```

---

# 三、本阶段修改文件
本阶段主要修改两个文件：

```latex
backend/rbac/views.py
backend/rbac/urls.py
```

---

# 四、修改 `views.py`
打开文件：

```latex
backend/rbac/views.py
```

---

## 第 1 步：修改导入
找到文件顶部的导入部分。

你现在应该有类似：

```python
import json

import jwt
from django.views import View
```

把它改成：

```python
import json
from datetime import datetime, date
from decimal import Decimal, InvalidOperation

import jwt
from django.views import View
```

然后找到这行：

```python
from .models import SysUser, SysRole
```

改成：

```python
from .models import SysUser, SysRole, SysUserProfile
```

---

## 第 2 步：在 `views.py` 里追加工具函数
在 `get_json_body` 函数下面，追加下面代码。

路径：

```latex
backend/rbac/views.py
```

追加代码：

```python
def format_date_value(value):
    """
    把日期格式化成字符串
    """

    if value is None:
        return None

    return value.strftime("%Y-%m-%d")


def format_datetime_value(value):
    """
    把日期时间格式化成字符串
    """

    if value is None:
        return None

    return value.strftime("%Y-%m-%d %H:%M:%S")


def format_decimal_value(value):
    """
    把 Decimal 转成字符串
    """

    if value is None:
        return None

    return str(value)


def parse_date_value(value, field_name):
    """
    解析日期字段

    前端传入格式：
    2026-05-26
    """

    if value in [None, ""]:
        return None

    try:
        return date.fromisoformat(value)
    except ValueError:
        raise ValueError(f"{field_name} 格式错误，正确格式是 YYYY-MM-DD")


def parse_datetime_value(value, field_name):
    """
    解析日期时间字段

    前端传入格式：
    2026-05-26 10:30:00
    或：
    2026-05-26T10:30:00
    """

    if value in [None, ""]:
        return None

    try:
        value = value.replace("T", " ")
        return datetime.fromisoformat(value)
    except ValueError:
        raise ValueError(f"{field_name} 格式错误，正确格式是 YYYY-MM-DD HH:MM:SS")


def parse_decimal_value(value, field_name):
    """
    解析小数字段
    """

    if value in [None, ""]:
        return None

    try:
        return Decimal(str(value))
    except InvalidOperation:
        raise ValueError(f"{field_name} 必须是数字")


def serialize_profile(profile):
    """
    把用户档案对象转换成 JSON 字典
    """

    user = profile.user

    return {
        "profile_id": profile.profile_id,
        "user_id": user.user_id,
        "username": user.username,
        "real_name": user.real_name,

        "level": profile.level,
        "gender": profile.gender,
        "gender_text": profile.get_gender_display() if profile.gender is not None else None,
        "birthday": format_date_value(profile.birthday),
        "height_cm": format_decimal_value(profile.height_cm),
        "weight_kg": format_decimal_value(profile.weight_kg),
        "avatar_url": profile.avatar_url,
        "intro": profile.intro,
        "create_time": format_datetime_value(profile.create_time),
        "join_time": format_datetime_value(profile.join_time),
        "expire_date": format_date_value(profile.expire_date),
    }


def get_or_create_user_profile(user):
    """
    获取当前用户档案

    如果没有档案，就自动创建一条空档案。
    """

    profile = SysUserProfile.objects.filter(
        user=user
    ).order_by("profile_id").first()

    if profile:
        return profile

    profile = SysUserProfile.objects.create(
        user=user,
        create_time=datetime.now(),
    )

    return profile
```

---

# 五、解释这些工具函数
## 1. `serialize_profile`
作用是把数据库对象转成 JSON。

因为数据库对象不能直接返回给前端，所以要转成：

```latex
字典 dict
```

例如：

```python
serialize_profile(profile)
```

会变成：

```json
{
  "profile_id": 1,
  "username": "admin",
  "level": 10
}
```

---

## 2. `get_or_create_user_profile`
作用是：

```latex
如果用户有档案，就返回已有档案
如果用户没有档案，就自动创建一条空档案
```

这样前端调用查询接口时，不会因为没有档案而报错。

---

## 3. `parse_date_value`
专门处理日期字段，比如：

```latex
birthday
expire_date
```

前端传：

```latex
1998-01-01
```

后端转成 Python 的日期对象。

---

## 4. `parse_datetime_value`
专门处理日期时间字段，比如：

```latex
create_time
join_time
```

前端传：

```latex
2026-05-26 10:30:00
```

后端转成 Python 的日期时间对象。

---

# 六、继续在 `views.py` 添加档案接口
在 `views.py` 文件最下面追加下面这个类。

路径：

```latex
backend/rbac/views.py
```

追加代码：

```python
@method_decorator(csrf_exempt, name="dispatch")
class ProfileMeView(View):
    """
    当前登录用户档案接口

    GET /api/profile/me/   查询当前用户档案
    PUT /api/profile/me/   修改当前用户档案
    """

    @login_required
    def get(self, request):
        user = request.current_user

        profile = get_or_create_user_profile(user)

        return json_response(
            data=serialize_profile(profile),
            message="获取档案成功",
        )

    @login_required
    def put(self, request):
        user = request.current_user

        data = get_json_body(request)

        if data is None:
            return json_response(
                message="请求体不是合法 JSON",
                code=400,
                status=400,
            )

        profile = get_or_create_user_profile(user)

        try:
            if "level" in data:
                level = data.get("level")
                profile.level = None if level in [None, ""] else int(level)

            if "gender" in data:
                gender = data.get("gender")

                if gender in [None, ""]:
                    profile.gender = None
                else:
                    gender = int(gender)

                    if gender not in [0, 1, 2]:
                        return json_response(
                            message="gender 只能是 0、1、2",
                            code=400,
                            status=400,
                        )

                    profile.gender = gender

            if "birthday" in data:
                profile.birthday = parse_date_value(
                    data.get("birthday"),
                    "birthday"
                )

            if "height_cm" in data:
                profile.height_cm = parse_decimal_value(
                    data.get("height_cm"),
                    "height_cm"
                )

            if "weight_kg" in data:
                profile.weight_kg = parse_decimal_value(
                    data.get("weight_kg"),
                    "weight_kg"
                )

            if "avatar_url" in data:
                avatar_url = data.get("avatar_url")

                if avatar_url and len(avatar_url) > 255:
                    return json_response(
                        message="avatar_url 长度不能超过 255",
                        code=400,
                        status=400,
                    )

                profile.avatar_url = avatar_url

            if "intro" in data:
                profile.intro = data.get("intro")

            if "create_time" in data:
                profile.create_time = parse_datetime_value(
                    data.get("create_time"),
                    "create_time"
                )

            if "join_time" in data:
                profile.join_time = parse_datetime_value(
                    data.get("join_time"),
                    "join_time"
                )

            if "expire_date" in data:
                profile.expire_date = parse_date_value(
                    data.get("expire_date"),
                    "expire_date"
                )

        except ValueError as error:
            return json_response(
                message=str(error),
                code=400,
                status=400,
            )

        profile.save()

        return json_response(
            data=serialize_profile(profile),
            message="修改档案成功",
        )
```

---

# 七、这个接口做了什么？
## 1. 查询档案
```latex
GET /api/profile/me/
```

流程：

```latex
1. 检查 access_token
2. 解析当前用户
3. 查询 sys_user_profile
4. 如果没有档案，自动创建一条
5. 返回档案信息
```

---

## 2. 修改档案
```latex
PUT /api/profile/me/
```

流程：

```latex
1. 检查 access_token
2. 解析当前用户
3. 获取请求体 JSON
4. 查询当前用户档案
5. 修改 level、gender、birthday 等字段
6. 保存到数据库
7. 返回修改后的档案
```

---

# 八、修改 `urls.py`
打开文件：

```latex
backend/rbac/urls.py
```

找到导入部分。

原来可能是：

```python
from .views import (
    LoginView,
    RefreshTokenView,
    MeView,
    UserListView,
    UserDetailView,
)
```

改成：

```python
from .views import (
    LoginView,
    RefreshTokenView,
    MeView,
    UserListView,
    UserDetailView,
    ProfileMeView,
)
```

然后在 `urlpatterns` 里面追加：

```python
path("profile/me/", ProfileMeView.as_view()),
```

完整示例：

```python
from django.urls import path

from .views import (
    LoginView,
    RefreshTokenView,
    MeView,
    UserListView,
    UserDetailView,
    ProfileMeView,
)

urlpatterns = [
    path("login/", LoginView.as_view()),
    path("token/refresh/", RefreshTokenView.as_view()),

    path("me/", MeView.as_view()),

    path("users/", UserListView.as_view()),
    path("users/<int:user_id>/", UserDetailView.as_view()),

    path("profile/me/", ProfileMeView.as_view()),
]
```

最终新增接口地址是：

```latex
GET  /api/profile/me/
PUT  /api/profile/me/
```

---

# 九、启动后端
确认你在：

```latex
rbac_project/backend
```

启动后端：

```bash
python manage.py runserver
```

---

# 十、测试接口
## 第 1 步：登录 admin 获取 token
macOS / Linux：

```bash
curl -X POST http://127.0.0.1:8000/api/login/ \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'
```

Windows PowerShell：

```powershell
curl -Method POST "http://127.0.0.1:8000/api/login/" `
  -Headers @{"Content-Type"="application/json"} `
  -Body '{"username":"admin","password":"123456"}'
```

复制返回结果里的：

```latex
access_token
```

下面用它替换：

```latex
你的admin_access_token
```

---

## 第 2 步：查询当前用户档案
macOS / Linux：

```bash
curl -X GET http://127.0.0.1:8000/api/profile/me/ \
  -H "Authorization: Bearer 你的admin_access_token"
```

Windows PowerShell：

```powershell
curl -Method GET "http://127.0.0.1:8000/api/profile/me/" `
  -Headers @{"Authorization"="Bearer 你的admin_access_token"}
```

如果当前用户没有档案，系统会自动创建一条空档案。

正常返回：

```json
{
  "code": 200,
  "message": "获取档案成功",
  "data": {
    "profile_id": 1,
    "user_id": 1,
    "username": "admin",
    "real_name": "管理员",
    "level": null,
    "gender": null,
    "gender_text": null,
    "birthday": null,
    "height_cm": null,
    "weight_kg": null,
    "avatar_url": null,
    "intro": null,
    "create_time": "2026-05-26 10:00:00",
    "join_time": null,
    "expire_date": null
  }
}
```

---

## 第 3 步：修改当前用户档案
macOS / Linux：

```bash
curl -X PUT http://127.0.0.1:8000/api/profile/me/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 你的admin_access_token" \
  -d '{
    "level": 10,
    "gender": 1,
    "birthday": "1998-01-01",
    "height_cm": "175.50",
    "weight_kg": "70.25",
    "avatar_url": "https://example.com/avatar.png",
    "intro": "这是管理员的个人简介",
    "join_time": "2026-05-26 10:30:00",
    "expire_date": "2030-12-31"
  }'
```

Windows PowerShell：

```powershell
curl -Method PUT "http://127.0.0.1:8000/api/profile/me/" `
  -Headers @{
    "Content-Type"="application/json"
    "Authorization"="Bearer 你的admin_access_token"
  } `
  -Body '{
    "level": 10,
    "gender": 1,
    "birthday": "1998-01-01",
    "height_cm": "175.50",
    "weight_kg": "70.25",
    "avatar_url": "https://example.com/avatar.png",
    "intro": "这是管理员的个人简介",
    "join_time": "2026-05-26 10:30:00",
    "expire_date": "2030-12-31"
  }'
```

正常返回：

```json
{
  "code": 200,
  "message": "修改档案成功",
  "data": {
    "profile_id": 1,
    "user_id": 1,
    "username": "admin",
    "real_name": "管理员",
    "level": 10,
    "gender": 1,
    "gender_text": "男",
    "birthday": "1998-01-01",
    "height_cm": "175.50",
    "weight_kg": "70.25",
    "avatar_url": "https://example.com/avatar.png",
    "intro": "这是管理员的个人简介",
    "create_time": "2026-05-26 10:00:00",
    "join_time": "2026-05-26 10:30:00",
    "expire_date": "2030-12-31"
  }
}
```

---

# 十一、测试普通用户 zhangsan
登录：

```bash
curl -X POST http://127.0.0.1:8000/api/login/ \
  -H "Content-Type: application/json" \
  -d '{"username":"zhangsan","password":"123456"}'
```

拿到 zhangsan 的 `access_token` 后，请求：

```bash
curl -X GET http://127.0.0.1:8000/api/profile/me/ \
  -H "Authorization: Bearer 你的zhangsan_access_token"
```

zhangsan 查询到的是自己的档案。

再修改：

```bash
curl -X PUT http://127.0.0.1:8000/api/profile/me/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 你的zhangsan_access_token" \
  -d '{
    "level": 1,
    "gender": 1,
    "birthday": "2000-05-01",
    "height_cm": "172.00",
    "weight_kg": "65.00",
    "intro": "我是普通用户张三"
  }'
```

这说明：

```latex
每个用户只能操作自己的档案
```

---

# 十二、不带 token 的情况
直接请求：

```bash
curl -X GET http://127.0.0.1:8000/api/profile/me/
```

应该返回：

```json
{
  "code": 401,
  "message": "未提供 access_token",
  "data": null
}
```

说明这个接口已经受到登录保护。

---

# 十三、数据格式说明
## 1. gender
```latex
0：未知
1：男
2：女
```

---

## 2. birthday
格式必须是：

```latex
YYYY-MM-DD
```

例如：

```latex
1998-01-01
```

---

## 3. join_time
格式建议是：

```latex
YYYY-MM-DD HH:MM:SS
```

例如：

```latex
2026-05-26 10:30:00
```

也可以传：

```latex
2026-05-26T10:30:00
```

---

## 4. height_cm / weight_kg
可以传字符串或数字。

例如：

```json
{
  "height_cm": "175.50",
  "weight_kg": "70.25"
}
```

---

# 十四、用 MySQL 检查数据
进入 MySQL：

```bash
mysql -u root -p
```

选择数据库：

```sql
USE rbac_db;
```

查询档案：

```sql
SELECT
    p.profile_id,
    u.username,
    u.real_name,
    p.level,
    p.gender,
    p.birthday,
    p.height_cm,
    p.weight_kg,
    p.avatar_url,
    p.intro,
    p.create_time,
    p.join_time,
    p.expire_date
FROM sys_user_profile p
JOIN sys_user u ON p.user_id = u.user_id;
```

你应该能看到 admin 或 zhangsan 的档案数据。

---

# 十五、阶段 11 完成标准
你需要确认：

```latex
1. views.py 已经导入 SysUserProfile
2. views.py 已经新增 ProfileMeView
3. urls.py 已经新增 path("profile/me/", ProfileMeView.as_view())
4. 不带 token 请求 /api/profile/me/ 返回 401
5. 带 admin token 请求 GET /api/profile/me/ 成功
6. 带 admin token 请求 PUT /api/profile/me/ 可以修改档案
7. 带 zhangsan token 请求 GET /api/profile/me/ 查询的是 zhangsan 自己的档案
8. MySQL 的 sys_user_profile 表里能看到档案数据
```

---

# 十六、当前完整关系
现在你的系统关系变成：

```latex
权限关系：
sys_user -> sys_role -> sys_role_permission -> sys_permission

档案关系：
sys_user -> sys_user_profile
```

也就是说：

```latex
JWT 负责确认当前用户是谁
RBAC 负责判断用户有没有权限
Profile 接口负责管理当前用户自己的扩展档案
```

到这里，**阶段 11：用户档案查询和修改接口**完成。

